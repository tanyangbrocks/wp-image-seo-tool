# WP Image SEO Tool 實作進度

*最後更新：2026-07-14*

## 目前狀態

靜態網頁工具核心功能已完成：使用者上傳圖片 → 進入原本的辨識/編輯/產生 HTML 流程。**2026-07-14 新增著陸頁＋捲動流程＋修一個真的 OCR bug**：頁面一載入先看到全螢幕的著陸頁（大標題/副標＋「選擇圖片」「調整辨識語言與引擎」兩個按鈕＋置中的 Created by 字樣，字樣右側加圖示），選圖後才平滑捲動到下面原本的工作區（工作區拿掉了原本疊在最上面的標題/副標/設定按鈕，改成左上角一個「← 上一步」，點了會跳出「是／否」確認視窗，選是才清空狀態、捲回著陸頁重新選圖）；驗證這個新流程時意外揪出一個**真的 OCR bug**——`ppu-paddle-ocr` 這個第三方套件在同一個 `PaddleOcrService` 實例上連續辨識兩張「像素尺寸完全相同」的圖片時，第二次會直接回傳第一次的舊結果（跟畫面內容無關，是套件內部依尺寸做的快取/重用邏輯有問題），已用幫每次辨識的畫布加一點點會變動的留白邊界繞過，詳見下方「最新完成」。**同日再微調回彈手感**：使用者回報頂/底端回彈效果「停滯感」太久，把 `js/scroll-effects.js` 的 `MAX_HOLD_MS`（持續下拉時最長撐住多久才強制彈回）從 400ms 減半到 200ms。**2026-07-13 字距/字級聯合自動貼合**：使用者追問「覆蓋文字有沒有調整字距」，答案是沒有——之前的自動貼合只調字級、字距永遠是 0，於是重寫成同時決定兩者：`js/text-fit.js` 的 `fitTextToBox()` 先用 `canvas.measureText()` 的 `actualBoundingBoxAscent/Descent` 量真實字高、依框高反推字級，再用該字級量出文字自然寬度、拿寬度差反推字距（而不是像以前只靠縮小字級去湊寬度）；`js/line-merge.js` 拿掉自己算行距的邏輯，改成保留 `lineCount` 交給 `js/main.js` 在合併*之後*才對每個（可能合併過的）方塊算字級/字距/行距——因為合併後的方塊尺寸/文字內容才是最終真正要貼合的目標，合併前對單行算的字級沒有意義；多列合併的方塊還多做一次二次擬合（字級↔行距互相依賴，先用預設值估一次、算出真實行距後再用行距重新擬合字級一次），詳見下方「最新完成」。**2026-07-13 頭尾視覺微調**：使用者對照正式站截圖回報 5 個小調整——Created by 旁的頭像圖示加圓形外框、副標字級跟主標一致、主副標整行放大 1.5 倍（24px→36px）、「產生 HTML」按鈕靠齊區塊右下角並放大 1.5 倍、Created by 文字改粗體，皆為 `css/style.css` 純樣式調整。**2026-07-13 OCR/疊字優化報告＋落地 4 項**：先寫一份調查報告（[docs/report-ocr-overlay-optimization.md](docs/report-ocr-overlay-optimization.md)，派 agent 上網查證後彙整）盤點 OCR 與疊字生成機制的優化空間，再把報告裡標記「低成本、高信心」的 4 項直接做掉：疊字字級改用 `canvas.measureText()` 自動貼合 OCR 框寬（取代舊的固定 0.85 高度係數）、Google Vision 補上信心分數過濾（原本三引擎裡唯一沒有這層防護）、載入 Noto Sans TC 可變字重字體取代寫死的系統字體、新增漸層色文字偵測＋`background-clip:text` 還原，詳見下方「最新完成」。**2026-07-13 加捲動動畫**：比照 `C:\Portfolio` 加兩個捲動互動——區塊隨捲動進出視窗淡入淡出、捲到頁面頂/底端繼續滑會拉出橡皮筋回彈效果，皆為原生 JS/CSS 實作（`js/scroll-effects.js` 新），詳見下方「最新完成」；順便把辨識引擎/語言設定選單裡過長的說明文字精簡（PaddleOCR 只留「（預設）」、Tesseract 拿掉括號說明、書寫系統標籤拿掉括號說明）。**2026-07-13 修文字對齊根因＋字距行距＋自動合併相近行**：修掉一個確實存在的疊字垂直位移 bug（右欄預覽畫布與最終產生的 HTML 都受影響，見下方「最新完成」）；標題主副標合併成一行；編輯視窗新增「字距」「行距」拉桿；OCR 偵測到的相近文字行（同段落被拆成多行）現在會自動合併成一個可編輯的多行文字方塊，而不是好幾個分開的方塊。**2026-07-13 再次微調**：標題列右側把「Created by 彈羊如柳丁」與設定按鈕收在一起置中；左欄縮圖一開始就撐滿版面不留空白，雙欄用 `align-items:stretch` 等高；右欄從「立即可編輯」改回「唯讀預覽＋按鈕/雙擊才開編輯視窗」（見下方「最新完成」，是對前一版「即時可編輯畫布」的局部復原，唯編輯視窗內部仍維持即時生效不做存檔/取消）；Alt 文字區塊改回半版寬不撐滿整行；縮放把手改實心縮小；拉桿文案「透明度」訂正為「不透明度」；`assets/icon/orange-sheep.png` 設為 favicon 並加在 Created by 那行文字右側。**2026-07-13 介面大改版**：左右兩欄佈局（左：上傳+純縮圖；右：即時可編輯畫布）、辨識引擎/語言設定收合成預設關閉的浮動選單、編輯即時生效（拿掉先前的儲存/取消模型）、方塊互動整個重做——移動把手＋四角縮放把手＋文字永遠可直接點擊編輯，三者互不干擾，修掉舊版「點擊常被誤判成拖曳」導致的打不了字/方塊黏游標問題；右上角加「Created by 彈羊如柳丁」。詳見「最新完成」表格，舊版彈窗式編輯器設計記錄在 [docs/plan-manual-overlay-editor.md](docs/plan-manual-overlay-editor.md)（已標記被取代）。**同日再加視覺主題**：標題改成「讓圖片文字可選」/副標「令圖片上每個文字都被 WordPress 看見」，拿掉原本最上面的說明段落；主色調/醒目色調配色跟 `C:\Portfolio` 淺色模式一致（暖米白背景 `#FBF3DC`＋橄欖金醒目色 `#7E6725`），區塊標題與按鈕文字用醒目色，其餘一般文字色；字體統一微軟正黑體；比照 Portfolio 的按鈕彈跳互動（按下 `scale(0.92)` 快速回彈、放開用 overshoot 曲線彈回）。**同日再優化產生的 HTML 本身的 SEO/GEO**：`<img>` 加上 `width`/`height`（避免版面跳動）、`loading="lazy"`/`decoding="async"`；外層從裸 `<div>` 改成 `<figure>`+可見的 `<figcaption>`（跟 alt 同一份 Alt 文字，但可見的 caption 是比 alt 更強的圖片排名訊號）；新增 `ImageObject` JSON-LD 結構化資料給 Google 圖片搜尋/AI answer engine 明確的圖片描述；輸出區塊上方加使用者可見的提醒——base64 內嵌圖無法被圖片搜尋索引，貼上 WordPress 後要把 `<img>` 網址換成媒體庫真實網址（這是架構限制，工具本身不能幫使用者上傳到 WordPress）。**同日再做一次全專案 bug 稽核**：新增 `scripts/precheck.js`（純 Node 內建模組，機械式比對 DOM id/import-export/檔案是否存在/HTML 標籤配對/CSS 死選擇器，任何人之後都能重跑）+ [docs/manual-review-checklist.md](docs/manual-review-checklist.md)（記錄靜態腳本抓不到、但純讀程式碼推理就能找到的 bug 類型，之後改完大功能可以重過一輪）。這輪抓到並修掉一個真的競態條件（連續上傳兩張圖，前一張還沒辨識完就傳第二張，舊版會讓先完成的那次結果覆蓋畫面）、圖片載入失敗沒有錯誤處理會卡死在「載入圖片中」、複製按鈕的 clipboard API 失敗沒有任何回饋、方塊可以被拖出畫布外抓不回來、`setPointerCapture` 在特定情況會 throw 導致整次拖曳完全失效（驗證拖曳夾限修復時意外抓到的）、圖示按鈕缺 `aria-label`、純鍵盤使用者無法移動文字方塊、刪除方塊後焦點消失等問題，詳見「最新完成」表格與 checklist 文件。**辨識引擎 2026-07-12 起改成三選一**：預設 **PaddleOCR**（`ppu-paddle-ocr`，離線免帳號，實測對照片紋理背景的抵抗力遠優於 Tesseract，見下方「最新完成」與 [docs/plan-paddleocr-evaluation.md](docs/plan-paddleocr-evaluation.md)）、備選 Tesseract.js（離線，原本的引擎）、備選 Google Cloud Vision（需自己的 API 金鑰，準確度最高但要連 Google 伺服器），Vision 辨識失敗時自動 fallback 回 PaddleOCR。金鑰只存在使用者自己瀏覽器的 localStorage，不會寫進輸出的 HTML 或 commit 進 repo。**2026-07-13 加上背景預載**：頁面一載入（若目前選的引擎是預設的 PaddleOCR）就在背景開始下載+初始化模型，不用等使用者真的上傳圖片才觸發，實測讓「頁面載入後等幾秒再上傳」情境下的辨識時間從原本冷啟動的數秒級降到 224ms；同時在 `<head>` 加 PaddleOCR 用到的三個外部主機（`cdn.jsdelivr.net`／`media.githubusercontent.com`／`raw.githubusercontent.com`）的 `preconnect`，見下方「最新完成」。Tesseract 路徑仍保留信心分數過濾（`MIN_LINE_CONFIDENCE=60`），解決它誤把照片紋理雜訊辨識成大量假文字行的問題；PaddleOCR 路徑實測不需要這層過濾也不會誤判（見 Phase A 結果）。**2026-07-12 架構重構**：原本的單一 `index.html` 已拆成 `css/style.css` + `js/*.js`（原生 ES modules，無 bundler），為 PaddleOCR 整合與即將進行的手動編輯介面預先鋪路，見「最新完成」表格與 [CLAUDE.md](CLAUDE.md) 架構說明。

GitHub repo 已建立並 push，Vercel 部署使用者已自行連接（Import from GitHub，Project Name `wp-image-seo-tool`，Framework Preset `Other`，Root Directory `./`，零建置流程），已確認正式環境正常運作。使用者提出三個改善建議：語言選擇選單、Alt 改成按鈕觸發+WordPress 渲染預覽、要不要自建 AI 模型——前兩項已實作，第三項已提供不建議自建模型的分析（現有 Google Vision 選項已是更划算的「換更強模型」槓桿）。後續討論「架伺服器用 EasyOCR/MMOCR/docTR」的可行性後，改朝「PaddleOCR 瀏覽器版」方向調查，已完成調查+列計畫+使用者回覆 3 個待確認事項+執行完整 Phase A~D（見 [docs/plan-paddleocr-evaluation.md](docs/plan-paddleocr-evaluation.md)），**PaddleOCR 整合已完成並已成為預設引擎**，詳見「最新完成」表格。使用者另外提出 WordPress 預覽區要加手動編輯介面（透明度拉桿+文字方塊拖曳編輯+全域透明開關），已列計畫+使用者回覆 5 個待確認事項+執行完整 Phase A~G（見 [docs/plan-manual-overlay-editor.md](docs/plan-manual-overlay-editor.md)），**手動編輯介面已完成**，詳見「最新完成」表格。

## 最新完成

> 歷史紀錄：[docs/history/completed.md](docs/history/completed.md)（超過 5 筆自動歸檔，見 [docs/archive-done.ps1](docs/archive-done.ps1)）

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| 著陸頁＋捲動流程＋「上一步」確認回退＋修一個真的 PaddleOCR bug | `index.html`、`css/style.css`、`js/main.js`、`js/scroll-effects.js`、`js/ocr-paddle.js` | 使用者提供 UI 草圖，要求頁面一載入先看到全螢幕著陸頁（大標題/副標＋兩顆按鈕＋置中 Created by），選圖後平滑捲動到下面的工作區，工作區拿掉原本疊在最上面的標題/副標/設定按鈕（因為都搬到著陸頁了），改成左上角一個「← 上一步」，點了跳出是/否確認視窗，確認才清空狀態並捲回著陸頁；另外 icon 外框改用醒目色、著陸頁 Created by 右側加圖示且整行置中。**HTML/CSS 改動**：`index.html` 新增 `<section id="landingSection">`（`min-height:100vh` 垂直置中）包住原本的標題/副標/settings 按鈕（沿用同一組 `#settingsToggleBtn`/`#settingsPanel`，只是搬家＋樣式改用新的 `.landingBtn` class），新增 `#landingChooseBtn`（純粹 proxy 到既有 `#imageInput.click()`，不重複造一個檔案選擇器）；原本 `.mainGrid`/`.bottomRow`/`#outputWrap` 整個包進新的 `<section id="workspaceSection">`，最上面加 `#backBtn`；新增 `<dialog id="backConfirmDialog">`（是/否兩顆按鈕）。`.popoverPanel` 定位從 `right:0`（原本貼右上角）改成 `left:50%;transform:translateX(-50%)`（現在按鈕在頁面正中間），連帶修正進場動畫 `@keyframes fadeSlideDown` 沒帶 `translateX(-50%)` 會在動畫過程中跳掉置中效果的問題。**JS 邏輯**：`js/main.js` 新增 `resetWorkspace()`（重用既有 `uploadToken` 遞增機制擋掉重置當下還在跑的 OCR）＋ `backBtn`/`backConfirmYes`/`backConfirmNo` 事件；`imageInput` 的 `change` handler 一開始（驗證失敗也一樣）就呼叫 `workspaceSection.scrollIntoView({behavior:'smooth'})`，讓設定錯誤訊息（原本在著陸頁看不到，因為 `#ocrStatus` 在工作區裡）也會捲到看得見的地方。`js/scroll-effects.js` 的 `IntersectionObserver` 目標清單拿掉已經不存在的 `.headerRow`。**驗證這個新流程時意外抓到一個真的 OCR bug（跟這次的 UI 改動本身無關，是既有程式碼的問題，只是這次的「上一步後重新上傳」流程第一次讓它被踩到）**：連續上傳兩張「像素尺寸完全相同」的圖片時，`ppu-paddle-ocr`（第三方套件）在同一個快取的 `PaddleOcrService` 實例上第二次呼叫 `recognize()` 會直接回傳第一次的舊辨識結果，跟第二張圖片實際內容無關——用合成測試圖直接證實：連續兩張 400×200 但文字不同的圖，第二張辨識結果幾乎是原封不動的第一張文字；換一個不同尺寸（550×260）的第三張圖則辨識正確。**不是這個專案自己 `getService()` 快取邏輯的 bug**（那層快取只快取「服務初始化」，canvas 每次都重新建立、內容也正確畫上新圖），是套件內部某種依尺寸做的快取/緩衝區重用邏輯有問題。**修法**：不能整個拿掉服務快取（那會讓每次上傳都要重新跑一次模型初始化，正是當初特地快取的原因），改成每次呼叫 `recognize()` 前，把畫布用一個每次呼叫都會變化的留白邊界（1~8px 循環，只加在右/下邊，不影響左上角座標系統，因此不會偏移任何已辨識文字的座標）撐大一點，讓連續兩次呼叫幾乎不可能撞到同一個尺寸；留白本身是純白色，這個專案自己在 Phase A 已經實測過 PaddleOCR 不會把空白/無紋理區域誤判成文字。**驗證方式**：合成測試——連續三次上傳「完全相同尺寸」的圖片（各自文字不同），修好之後三次都正確顯示各自的新文字（改之前卡在第一次的結果不會變）；確認 padding 沒有讓偵測框座標跑掉（`left`/`top`/`width`/`height` 百分比數值仍在合理範圍）；完整跑一次「著陸頁選圖 → 捲到工作區 → 上一步 → 是 → 捲回著陸頁 → 再次選圖（含 proxy 按鈕路徑）」全流程，確認狀態正確重置、重新上傳正確顯示新結果；settings 彈出視窗置中對齊、圖示圓框改醒目色、Created by 置中且圖示在「丁」右側，皆用 `getBoundingClientRect()`/`getComputedStyle()` 核對正確；`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。 |
| 字距/字級聯合自動貼合＋合併後才擬合（不再只調字級、字距永遠 0） | `js/text-fit.js`（重寫）、`js/line-merge.js`、`js/main.js`、`js/html-builder.js` | 使用者問「覆蓋文字時有在調整文字間距嗎」，答案是沒有——之前的 `fitFontSizeToBox()` 只搜尋字級，永遠假設字距 0，框的寬度完全靠縮放字級去湊，原圖字距比通用字體寬或窄時字級就會跟著失真。使用者接著重申「行距很接近的文字要視為同一個文字方塊，一個文字方塊可以有很多列」——檢查後合併鏈本身沒問題（`mergeCloseLines()` 每次合併會延伸 `prev` 的框，下一行接著跟延伸後的框比對，天生支援 3 行以上連續合併），但合併跟字級擬合的**執行順序**有問題：舊版在合併「之前」就對每一條原始 OCR 行算好字級，合併時只是簡單沿用第一行的字級，沒有針對合併後的最終框重新算。**改法**：`text-fit.js` 的 `fitTextToBox()` 重寫成兩步驟——① 用 `ctx.measureText()` 的 `actualBoundingBoxAscent`/`actualBoundingBoxDescent`（量真實字形高度，不是字體 em 方塊的通用高度）反推字級去貼合框高；② 用這個字級量文字「字距 0」時的自然寬度，跟框寬的差距反推每個字元間應該加的字距（超出字距滑桿原本的 `-0.05em~0.5em` 範圍就夾住，夾住後仍然太寬就退回縮小字級，維持「絕不裁切」的舊保證）。`line-merge.js` 拿掉自己算行距的邏輯（那段邏輯依賴當時還沒重新算過的字級，其實一直是錯的），只留幾何合併＋暴露 `lineCount`（合併了幾行）。`main.js` 把字級/字距/行距的計算整個搬到 `mergeCloseLines()` 之後，對每個（可能合併過的）方塊用**最終的框尺寸與合併後的完整文字**去擬合；多列方塊還多做一次二次擬合——字級與行距互相依賴（行距決定每列能分到多少高度，但行距本身要等字級擬合完才量得出來），先用內部預設值粗估一次、算出真實行距後再拿這個行距重新呼叫一次 `fitTextToBox()`，讓兩者收斂到更接近的數值。**驗證方式**：合成測試圖畫一行手動加寬字距的文字（模擬原圖字距明顯比通用字體寬），確認 `letterSpacing` 正確頂到滑桿上限 `0.5em` 且視覺沒有溢出（`scrollWidth===clientWidth`）；再畫三行緊貼在一起的文字，確認正確合併成一個三列文字方塊（`text` 含兩個 `\n`）、`lineHeight` 算出實際數值（非固定 1.05 預設）、二次擬合後數值有依預期方向收斂；編輯視窗的字距/行距/字級滑桿正確同步顯示這些自動算出的值；最終產生的 HTML 也帶著同樣的數值——順便發現並修掉一個小問題：`lineHeight`/`letterSpacing` 現在是量出來的浮點數（不是滑桿的整數步進值），沒四捨五入直接塞進 `style` 屬性會產生 17 位小數的醜字串，補上 `.toFixed(3)`。`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。**已知殘留誤差**：極端測試（手動字距拉很寬、三行擠很近）下量到方塊高度仍有 15~20% 的垂直溢出殘留（被 `overflow:hidden` 裁掉一點點），是 CSS 行距/字型內部 leading 跟純數學公式估計之間本來就有的落差，真實 OCR 圖片的行距不會這麼極端，暫不視為需要繼續深挖的問題。 |
| 頭尾視覺微調：logo 圓框／主副標同尺寸放大 1.5 倍／產生按鈕右下角放大 1.5 倍／Created by 粗體 | `css/style.css` | 使用者對照正式站截圖回報 5 個小調整，皆純 CSS：`.creditLogo`（Created by 旁的頭像圖示）`border-radius:4px`→`50%`＋新增 `border:1px solid var(--border)`，變成圓形外框徽章；`.subtitle` 字級從 `15px` 改成跟 `h1` 一樣；接著兩者一起放大 1.5 倍（`h1`/`.subtitle` 都是 `24px`→`36px`）；`.genBtn`（「產生 HTML」按鈕）`padding`/`font-size`/`border-radius` 等比放大 1.5 倍（`12px 24px`/`15px`/`6px` → `18px 36px`/`22.5px`/`9px`），`.bottomRow .genBtn` 的 `justify-self` 從 `start` 改成 `end`，搭配既有的 `align-items:end`，讓按鈕貼齊區塊右下角；`.credit`（Created by 那行字）加 `font-weight:700`。**驗證方式**：真實瀏覽器 `getComputedStyle` 逐項核對六個數值（`h1`/`.subtitle` 字級皆 `36px`、`.credit` 字重 `700`、`.creditLogo` 圓角/邊框正確）；`getBoundingClientRect()` 核對 `genBtn` 的右邊界／下邊界跟 `.bottomRow` 容器的右邊界／下邊界完全對齊（確認真的貼在右下角，不是只有大概對齊）；`node scripts/precheck.js` 0 錯誤；全程 console 無錯誤。 |
| OCR/疊字優化報告＋落地 4 項（自動貼合字級／Vision 信心過濾／Noto Sans TC／漸層文字） | `docs/report-ocr-overlay-optimization.md`（新）、`js/text-fit.js`（新）、`js/ocr-vision.js`、`js/color.js`、`js/main.js`、`js/preview.js`、`js/editor.js`、`js/html-builder.js`、`index.html`、`css/style.css` | 使用者要求盤點 OCR 與疊字生成機制的優化空間，「仔細思考、可以開 agent 查網路資源」。**方法**：先讀現有程式碼建立現況基準，派兩個 agent 平行上網查證（① OCR 引擎現況/前處理/信心過濾/旋轉文字處理，② 字體比對/文字自動貼合框寬/漸層文字/Web Font 選型），交叉比對後寫成 [docs/report-ocr-overlay-optimization.md](docs/report-ocr-overlay-optimization.md)，附優先順序表跟每項的信心度／成本評估。**最重要的發現**：這個工具會踩到的「照片紋理誤判成文字、花俏字體辨識不準」是整個 OCR 領域公開 benchmark（查到的 HalalBench 論文）都還沒解決的難題，不是換引擎就能解決，佐證目前選 PaddleOCR 的判斷仍合理。報告完成後，使用者要求「目前能做的都先做」，落地報告裡標記「低成本、高信心」的 4 項（跳過「非 ML 粗細判斷」——CJK 準確度存疑；跳過旋轉文字/ensemble/ONNX 字體分類——報告本身就建議暫緩，屬於中大型重構）：**① 疊字字級自動貼合框寬**：新增 `js/text-fit.js` 的 `fitFontSizeToBox()`，用 `canvas.measureText()` 對候選字級做二分搜尋，直到量出來的文字寬度貼近 OCR 框寬，取代 `js/main.js` 舊有的「框高度 × 0.85」固定經驗係數（完全不管字串/字體實際渲染寬度，這正是先前使用者反覆回報「疊字對不準」背後的另一個原因——box 位置對了，但文字寬度跟框寬不一定貼合，會溢出被裁或留白）。**② Google Vision 補信心過濾**：`js/ocr-vision.js` 從 `TEXT_DETECTION` 改成 `DOCUMENT_TEXT_DETECTION`（Google 文件保證這個偵測類型不用額外參數就會在 `fullTextAnnotation` 裡帶每個字詞的信心分數，`TEXT_DETECTION` 則要另外開參數且沒查證清楚會不會反映在舊版扁平陣列），新增 `MIN_CONFIDENCE_VISION=0.5` 過濾——修好三引擎裡唯一沒有信心過濾防護的缺口，但這個門檻**沒有真實金鑰可以實測校準**，明確標記是保守估計待之後調整。**③ 載入 Noto Sans TC**：`index.html` 加 Google Fonts CDN `<link>`（用官方 CDN 的網址讓 Google 自動依實際用到的字元切子集，不用自己下載整包可變字重字體造成 4-9MB 的檔案負擔），新增 `js/text-fit.js` 匯出的 `OVERLAY_FONT_STACK`／`OVERLAY_FONT_WEIGHT` 常數，css/style.css 的 `.previewLine`/`.ovBoxText` 跟 `html-builder.js` 產生的 inline style 三處都改用同一份字體堆疊（Noto Sans TC 優先，取代原本寫死的純系統字體），並在產生的 HTML 裡也附上同一個字體 CDN `<link>`，讓貼到 WordPress 後外觀跟預覽一致。**④ 漸層色文字偵測**：`js/color.js` 的 `extractTextColor()` 延伸——原本只抽一個平均色，現在額外把文字像素依 x 位置分成左/中/右三段，比較三段平均色是否有夠大的色差（`GRADIENT_MIN_DELTA=40`，沒有查到任何現成做法/文獻可以援引，這個門檻是自己估的），偵測到就回傳 `gradient` 陣列，三個渲染路徑改用新增的 `applyTextFillStyle()`（DOM 版，`preview.js`/`editor.js` 用）／`textFillCss()`（字串版，`html-builder.js` 用）套用 `background-clip:text` 漸層填色，偵測不到就照舊走單色路徑；使用者在編輯視窗手動選色時會自動清掉 `gradient`（明確覆蓋比自動偵測優先）；`.ovBoxText` 是 `contenteditable`，額外處理了漸層模式下 `caret-color` 會跟著 `color:transparent` 一起消失、打字看不到游標的問題。**驗證時抓到一個真的 bug**：`OVERLAY_FONT_STACK` 常數原本用雙引號包字型名稱（`"Noto Sans TC"`），但 `html-builder.js` 是直接把它塞進雙引號括起來的 HTML `style="..."` 屬性字串——字型名稱裡的雙引號會提前把這個屬性截斷，導致 `font-family` 之後的所有樣式（`color`／漸層／`font-size`／`text-shadow` 全部）憑空消失。**不是靠肉眼看出來的**，是拿 iframe 裡實際渲染節點的 `getAttribute('style')` 原始字串出來看，看到字串在 `font-family: ` 後面就被截斷才抓到；修法是字型堆疊常數改用單引號（CSS 合法、且跟外層雙引號不衝突），`css/style.css` 裡的兩處也一併改成單引號保持跟常數字面一致。**驗證方式**：`node scripts/precheck.js` 全程 0 錯誤；合成測試圖同時放一行純色文字＋一行 `ctx.createLinearGradient()` 畫的漸層文字，確認純色行 `scrollWidth===clientWidth`（自動貼合生效、沒有溢出/裁切）且顏色/字體正確、漸層行正確偵測出漸層並在唯讀預覽／編輯視窗／最終產生的 HTML 三處都渲染出對應的 `linear-gradient` 疊字；手動在編輯視窗改色，確認漸層正確被清掉、caret-color 正確重設；最終產生的 HTML 貼進獨立 iframe（`wpPreviewFrame`）重新解析渲染，逐一讀 `getComputedStyle` 核對顏色/漸層/字體都正確（抓到並修好上述雙引號截斷 bug 後才過）；全程 console 無錯誤。**明確跳過、留給使用者判斷的部分**：Google Vision 的信心門檻、漸層偵測的色差門檻都只是首版保守估計，沒有真實資料校準過；非 ML 粗細判斷（stroke width transform）評估後因為 CJK 準確度業界目前也只有 ~49%（查到的 YuzuMarker.FontDetection 專案數據）而跳過，沒有動手做。 |
| 捲動動畫：區塊進出視窗淡入淡出＋頂/底端橡皮筋回彈 | `index.html`、`css/style.css`、`js/scroll-effects.js`（新） | 使用者要求比照 `C:\Portfolio` 加「往下滑區塊淡出」與「觸碰到底部產生回彈」兩個捲動互動。**先查 Portfolio 實際怎麼做**（派 Explore agent 讀原始碼，不是憑印象猜）：① 淡出效果實際上不是連續跟著捲動百分比算透明度，而是 Framer Motion `whileInView({opacity:1}, {once:false})`——區塊離開視窗（不論往上或往下捲出）就淡出、捲回來再淡入，底層是 `IntersectionObserver`；② 回彈效果是獨立元件 `OverscrollBounce`（`C:\Portfolio\src\components\overscroll-bounce.tsx`），因為 Portfolio 用 Lenis 平滑捲動函式庫接管了捲動、瀏覽器原生的橡皮筋效果會被蓋掉，所以自己用 wheel/touch 事件手動重建：在頂/底端偵測到繼續往外捲的滾輪/觸控輸入時，把整個頁面用 Framer Motion 的 `useMotionValue` 拉出最多 80px（`RESISTANCE=0.4` 阻力、`DEAD_ZONE=10px` 死區），停止輸入 75ms 後用 `bounce:0.35` 的彈簧動畫彈回 0。**這個專案沒有 React/Framer Motion/Lenis，兩個效果都用原生 JS/CSS 重寫**，行為對齊但實作換一套：淡出用瀏覽器原生 `IntersectionObserver`（`rootMargin:'-40px'` 對應 Portfolio 的 `viewport:{margin:'-40px'}`），套用在四個主要區塊（標題列／上傳面板／預覽面板／Alt+產生按鈕列，`#outputWrap` 刻意不套——它自己已經有「產生 HTML」時的一次性進場動畫 `fadeSlideUp`，疊加第二個 opacity/transition 會互相打架）；回彈用 `#pageBounceWrap`（包住除了 `<dialog>` 外的整個頁面內容，dialog 走瀏覽器原生 top-layer 渲染，天生不受祖先 transform 影響，不需要特別排除）搭配純 `translateY` inline style，拖曳中即時跟手（`transition:none`）、放開後才切成 `transition:transform 500ms cubic-bezier(0.34,1.56,0.64,1)`——這條 overshoot 曲線是直接沿用這個專案既有的按鈕彈跳曲線（見上方「視覺主題」那筆），不是另外調的，維持全站彈跳手感一致；`html,body` 加 `overscroll-behavior:none` 蓋掉瀏覽器原生回彈，避免原生+自製雙重回彈。另外補一個 Portfolio 沒有的防呆：`<dialog>` 開啟時（`js/editor.js` 的編輯視窗）wheel/touch 事件直接略過，避免背景頁面在彈窗開著時還在回彈。**驗證方式與這次踩到的環境限制**：`node scripts/precheck.js` 0 錯誤；縮小視窗到 1000×500 讓頁面可以捲動，捲動邏輯本身（`atTop`/`atBottom` 邊界判斷、`wheel`/`touchmove` 事件是否正確攔截、`preventDefault()`、pull/dead-zone 數學、`<dialog>` 開啟時是否正確略過）都用合成 `WheelEvent`/直接呼叫驗證正確；但**這次測試瀏覽器工具的分頁完全不跑 render steps**（`requestAnimationFrame` 排的 callback 等了 1 秒也沒被呼叫，這不是這次新發現的孤例，跟本專案先前已經記錄過的「`<dialog>` close 事件不觸發」「`requestIdleCallback` 背景分頁被延後」屬於同一類分頁背景/非前景時的限制）——這代表**CSS transition 的動畫過程、`IntersectionObserver` 的 callback 本身都沒辦法在這個工具裡直接觀察到**（兩者都是瀏覽器 render steps 的一部分）。改用 `transition:none` 強制瞬間套用來驗證 CSS 規則本身正確（`.scrollFaded` 確實產生 `opacity:0; transform:translateY(16px)`，套用前後手動比對過），JS 判斷邏輯（class 有沒有正確被加上/拿掉、`translateY` 數值算得對不對、idle 後 `setTimeout` 是否正確觸發 `release()`）則不依賴 render steps、可以正常驗證且全部通過；**動畫本身流不流暢、`IntersectionObserver` 在真實捲動時會不會確實觸發**，都需要使用者實機用真瀏覽器確認（見下方待辦）。 |

## 待辦

- [ ] **使用者實機驗證著陸頁→工作區的平滑捲動動畫實際手感**——`scrollIntoView({behavior:'smooth'})` 的邏輯本身（捲到正確的目標、驗證失敗時也會捲動讓錯誤訊息看得到）已用 `behavior:'instant'` 等效測試驗證正確，但這個測試瀏覽器環境的分頁不會播放捲動動畫本身（跟本專案先前已記錄的 `requestAnimationFrame`/CSS transition 限制同一類），沒辦法在這裡直接看到滑動過程順不順
- [ ] 觀察 `js/ocr-paddle.js` 的留白邊界繞過法（同尺寸連續辨識會拿到舊結果的 bug）長期是否穩定——這是繞過第三方套件內部行為的作法，不是真正修好上游問題，如果之後升級 `ppu-paddle-ocr` 版本或套件自己修掉這個 bug，這段留白邏輯可以拿掉；且目前只驗證過連續 2~3 次同尺寸上傳，沒有壓力測試更多次連續同尺寸上傳（留白值 1~8px 循環，理論上 9 次以上會重複到同一個留白值，但只要不是「剛好連續兩次留白值也相同」就不會撞到，機率很低但非零）
- [ ] 使用者用真實行銷圖驗證字距/字級聯合自動貼合（`js/text-fit.js` 重寫版）的效果——合成測試（手動加寬字距、三行緊貼文字）已證實邏輯本身正確收斂，但真實圖片的字體特性/合併行距不會跟合成測試一樣極端，需要真實素材驗證觀感；另外極端案例下仍有 15~20% 的殘留垂直裁切誤差，真實圖片出現的機率待觀察
- [ ] 使用者用真的 Google Cloud Vision API 金鑰做一次實測（目前只驗證過請求格式正確與錯誤金鑰的 fallback 行為，沒有驗證過金鑰正確時的實際辨識品質）——**2026-07-13 已補上 `MIN_CONFIDENCE_VISION=0.5` 信心過濾**（見上方「最新完成」），但這個門檻沒有真實金鑰可以校準，是保守估計，需要實測確認會不會濾掉真文字或濾不掉雜訊
- [x] 確認 Vercel 部署狀態 — 2026-07-12 完成，`https://wp-image-seo-tool.vercel.app` 正常運作
- [ ] 持續用更多真實 AI 生圖測試 `MIN_LINE_CONFIDENCE = 60` 這個門檻是否需要調整（目前只用一組合成測試資料驗證過）
- [ ] 使用者實機驗證新的語言選單、按鈕觸發+WordPress 預覽功能手感（這次自動化測試因為分頁沒有系統焦點，複製按鈕沒辦法驗證到）
- [x] 是否要自建 AI 模型/訓練資料 — 2026-07-12 已提供分析，結論不建議（現有 Google Vision 選項已是更划算的「換更強模型」槓桿，自訓模型投資報酬率太差）
- [x] **PaddleOCR 瀏覽器版整合** — 2026-07-12 完成，`ppu-paddle-ocr` 已成為預設辨識引擎，Phase A~D 全部跑完，見 [docs/plan-paddleocr-evaluation.md](docs/plan-paddleocr-evaluation.md) 與上方「最新完成」
- [ ] 使用者實機用更多真實圖片（尤其是各種照片背景的行銷素材）驗證 PaddleOCR 的辨識品質與 `MIN_CONFIDENCE_PADDLE=0.3` 這個門檻是否需要調整（目前只用合成測試圖驗證過，跟當初 Tesseract 的 `MIN_LINE_CONFIDENCE=60` 一樣是憑一組測試資料抓的保守值）
- [ ] 使用者實機驗證新的三引擎選單（PaddleOCR/Tesseract/Google Vision）與書寫系統下拉選單手感
- [ ] （優先度較低的備選方案，暫緩）架伺服器 + EasyOCR/docTR 自架 OCR API：技術難度中等（半天~一天可做出堪用原型），但會讓專案從零維護的靜態網站變成要顧一台伺服器（要找 Render/Railway/Fly.io 等平台、可能要付費、要自己維運），先把 Google Vision 實測完 + PaddleOCR 瀏覽器版評估完，準確度還是不夠再考慮這條路。**MMOCR 不建議**，依賴環境複雜度明顯高於 EasyOCR/docTR，對這個規模的專案不划算
- [ ] （未來方向，非目前範圍）使用教學文件（可能開 `docs/` 資料夾放教學內容/截圖）、UI 介面擴充
- [x] **WordPress 預覽區手動編輯介面** — 2026-07-12 完成 Phase A~G；**2026-07-13 介面大改版時整個重做**（拿掉儲存/取消模型、方塊互動重新設計），見 [docs/plan-manual-overlay-editor.md](docs/plan-manual-overlay-editor.md)（已標記被取代）與上方「最新完成」
- [ ] **使用者實機用真滑鼠鍵盤驗證新版編輯畫布**，尤其是退格刪字——這次自動化測試環境的按鍵模擬對 `key BackSpace` 有普遍性限制（連原生 `<textarea>` 都無法用這個工具刪字），所以插入文字已證實修好，但退格只能從程式碼修法本身推論應該一併修好，沒辦法在這個環境直接證實
- [ ] 使用者實機確認雙欄佈局在各種螢幕寬度/實際手機瀏覽器下的呈現（目前只在 1280px 桌面尺寸測試過，`@media (max-width:720px)` 有寫但沒有在真手機上驗證）
- [ ] 使用者實機驗證新的「唯讀預覽＋按鈕/雙擊開編輯視窗」流程手感，尤其是 Esc 鍵關閉編輯視窗那條路徑（這次只驗證了「完成」按鈕關閉，因為測試瀏覽器環境 `<dialog>` 的 `close` 事件本身不會觸發，Esc 關閉走的正是這個事件，沒辦法在這個環境直接證實）
- [ ] 使用者實機用真實（非合成）行銷圖驗證文字對齊修復後的效果，尤其是原本就有多行文字、字體較特殊的圖——這次用 `canvas.fillText()` 合成測試圖精確驗證了座標轉換的兩個 bug 都修好，但沒有測過真實世界字體差異造成的視覺誤差（不同字體的字寬/字高比例跟疊字用的通用字體不同，理論上仍會有一定程度的視覺落差，不是這次能一併解決的範圍）
- [ ] 使用者實機驗證「自動合併相近 OCR 行」的合併門檻（垂直間距 <50% 平均行高、水平重疊 ≥40%）在真實圖片上是否恰當——目前只用合成測試圖驗證邏輯本身正確，真實 OCR 偵測到的行距/對齊誤差可能需要微調 `js/line-merge.js` 裡的兩個門檻常數
- [ ] **使用者實機驗證捲動動畫的實際流暢度**——區塊淡出/淡入（`IntersectionObserver`）跟頂/底端橡皮筋回彈的底層 JS 判斷邏輯都已用合成事件驗證正確，但動畫本身（CSS transition 播放、`IntersectionObserver` 在真實捲動下是否確實觸發）沒辦法在這次的測試瀏覽器工具裡直接看到——這個環境的分頁完全不跑 `requestAnimationFrame`，屬於環境限制不是程式碼問題，但仍需要真瀏覽器滑一次確認手感（尤其是觸控裝置的回彈阻力/死區數值 `RESISTANCE=0.4`／`DEAD_ZONE=10px` 是否需要微調）
- [ ] 使用者用真實行銷圖驗證新的疊字字級自動貼合（`js/text-fit.js`）效果——合成測試（純 Arial 畫的文字）已證實不會溢出/裁切，但真實圖片字體跟 Noto Sans TC 的字寬比例不會完全一樣，仍可能有一定程度的視覺落差，需要用真實素材看實際效果
- [ ] 使用者用真實圖片驗證漸層文字偵測的門檻（`GRADIENT_MIN_DELTA=40`）——完全沒有查到任何現成做法可以參考，是自己估的保守值，需要用真的漸層字行銷圖測會不會誤判一般單色文字成漸層、或反過來漏掉真的漸層

## 相關文件

- [docs/plan-paddleocr-evaluation.md](docs/plan-paddleocr-evaluation.md) — PaddleOCR 瀏覽器版可行性評估（網路調查、準確度比較數據、風險評估、分階段實作步驟）
- [docs/plan-manual-overlay-editor.md](docs/plan-manual-overlay-editor.md) — WordPress 預覽區手動編輯介面計畫（透明度拉桿、文字方塊拖曳編輯、全域透明開關、儲存重新產生 HTML）
