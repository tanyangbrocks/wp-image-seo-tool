# Completed Milestones History

Auto-archived from C:\wp-image-seo-tool\實作進度.md by docs/archive-done.ps1.

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| 建立專案治理檔案 + GitHub repo + Vercel 部署 | `CLAUDE.md`、`.gitignore`、`docs/archive-done.ps1`、`實作進度.md` | 依照 Portfolio / SkillCreatorUE5 兩個既有專案的工作慣例補齊：`CLAUDE.md` 記錄「單檔架構是刻意選擇、什麼情況才該拆檔」的判斷原則、🔴 API 金鑰絕對不能寫死進程式碼的安全規則（這個 repo 跟 Vercel 部署都公開）、核心 OCR/顏色萃取邏輯摘要、`DataTransfer` 測試技巧；`.gitignore` 排除 OS/編輯器暫存檔、`.vercel`、未來若加建置工具需要的 `node_modules`/`.env*`；`docs/archive-done.ps1` 從 Portfolio 專案原封不動複製（腳本本身完全通用，不綁定特定專案路徑）。新建 GitHub repo `tanyangbrocks/wp-image-seo-tool`，本地 `C:\wp-image-seo-tool\` push 上去；使用者自行完成 Vercel Import from GitHub 設定。 |
| 建立核心工具：OCR 文字位置/顏色還原 + Alt 驅動的即時 HTML 產生器 | `index.html` | 上傳圖片後用 Tesseract.js（`chi_tra+eng`）辨識文字內容與每一行的邊界框；用 Otsu 閾值法把框內像素分兩群（面積較小的一群視為文字本身），取該群**原始 RGB 平均色**還原文字真實顏色（不是只判斷黑白）；位置/大小換算成相對圖片 natural width/height 的百分比，字級用 CSS `container-type: inline-size` + `cqw` 單位維持縮放時的正確比例。Alt 欄位留空時不產生輸出，一填就即時（`input` 事件，非按鈕觸發）重新產生完整 HTML，複製貼上即可用。**進階設定**：使用者可選填自己的 Google Cloud Vision API 金鑰改走雲端辨識（`groupWordsIntoLines()` 把 Vision 回傳的逐字座標依 Y 座標鄰近度群組成行，模擬 Tesseract 原生的行結構，兩引擎共用同一套下游位置/顏色處理），金鑰錯誤或請求失敗時自動 fallback 回離線引擎；「記住金鑰」勾選後存本機 localStorage，不會外流。**驗證方式**：preview 工具用 `DataTransfer` + 手動 `dispatchEvent('change')` 模擬真實檔案上傳（原生檔案選擇對話框無法被自動化工具觸發），先後測試黑白文字圖與彩色文字圖（藍底紅字），確認辨識文字、位置百分比、顏色萃取（`rgb(230,40,40)` 原色抓出 `rgb(192,44,70)`，同色系）、Alt 即時產生/清空即消失、HTML 特殊字元跳脫（`"`/`<`/`>`/`&`）、複製按鈕都正確；用假金鑰測試確認 Google Vision 請求真的送達 Google 伺服器（收到結構化 400，非網路層級失敗）且正確 fallback，行為跟原本離線模式一致（回歸測試）。 |
| 修正真實照片背景誤辨識成大量假文字行 | `index.html` | 使用者用真實 AI 生圖（濃濃本家茶包海報，照片背景+少量文字）測試，回報偵測到 41 行文字、疊字疊得亂七八糟，畫面上全是「他 ol NoNG HoME」「xo ᴎ ヽy ᴎ」這種亂碼。**根因**：Tesseract 對照片紋理（木紋、布紋、食物表面反光）會信心滿滿地「辨識」出根本不存在的文字，因為它預設把整張圖當文件處理，沒有機制分辨「這裡真的有文字」還是「這裡只是視覺雜訊」。**診斷過程**：做了一張還原情境的測試圖（雜訊紋理背景+一行乾淨的真實文字），量測 `data.lines[].confidence`（0-100 信心分數），真文字拿到 88 分，26 個雜訊誤判全部落在 0-43 分——兩者之間有清楚的分隔帶。**修法**：新增 `MIN_LINE_CONFIDENCE = 60` 常數，Tesseract 路徑的行結果先過濾信心分數才進下游流程，低於門檻直接丟棄。**驗證方式**：同一張測試圖重跑，41→1 行（只剩真的那行），文字內容正確；另外拿先前驗證過的乾淨測試圖（黑白字、彩色字）重新回歸測試，結果跟修正前一模一樣，沒有誤殺真文字。**已知取捨**：60 分是憑一組測試資料抓出來的門檻，不是理論上的絕對值——如果之後有真實案例回報「明明有文字卻沒偵測到」，可能是文字本身很小/字體很潦草導致信心分數偏低，那時候要往下調整這個門檻，而不是整個拿掉這層過濾（拿掉會回到雜訊亂入的老問題）。Google Vision 路徑目前沒有套用同樣的信心過濾（該引擎本身對雜訊誤判的抵抗力好很多，且目前沒有真金鑰可以實測其信心分數欄位格式，先維持原樣）。 |
| 語言選擇選單 + 按鈕觸發產生 HTML + WordPress 渲染預覽 | `index.html` | 使用者提三個建議，實作前兩個：**(1) 語言選單**：新增可展開的語言勾選清單（繁中/簡中/英/日/韓/俄/德/法/西，預設勾繁中+英），`getSelectedLanguages()` 動態組出 Tesseract 語言字串（如 `eng+jpn`）與 Google Vision 的 `languageHints`（如 `['en','ja']`，兩套引擎的語言代碼系統不同，各自對照）；至少要勾一種語言才能上傳圖片，沒勾時擋在上傳前並顯示錯誤訊息，不會浪費一次辨識。**(2) 按鈕觸發 + 預覽**：Alt 欄位不再邊打邊即時產生 HTML（原本用 `input` 事件），改成輸入時只切換「產生 HTML」按鈕的 disabled 狀態，按下去才真正產生；產生的同時新增一個 `<iframe srcdoc="...">` 直接渲染**產生出來的那段 HTML 字串本身**（不是套用內部即時預覽狀態模擬出來的），如果 HTML 產生邏輯本身有跳脫/格式錯誤，這裡會如實呈現出來，比原本的即時疊字預覽更貼近「WordPress 貼上去後真正長怎樣」。**驗證方式**：`DataTransfer` 模擬上傳，確認語言清單渲染出全部 9 個選項且預設狀態正確；取消全部語言勾選後上傳圖片，確認被攔下並顯示錯誤訊息；重新勾選後確認 `getSelectedLanguages()` 正確反映勾選狀態；上傳測試圖後確認 Alt 打字只切換按鈕狀態、不會自動出 HTML；按下按鈕後確認 `htmlOutput`/`wpPreviewFrame.srcdoc` 都正確產生，且**直接讀取 iframe 的 `contentDocument`** 確認裡面的 `<img alt>`／疊字文字／顏色都跟預期一致（不是只檢查字串長度，是真的解析渲染結果）；OCR 辨識/信心過濾/顏色萃取邏輯本身沒有改動，用先前的測試圖回歸測試結果不變。**已知環境限制**：這次自動化測試環境 `document.hasFocus()` 是 `false`（瀏覽器分頁沒有真正的系統焦點），導致複製按鈕的 `navigator.clipboard.writeText()` 拋出 `NotAllowedError`——這是瀏覽器對非使用者主動觸發情境的標準安全限制，不是這次改動造成的迴歸（複製按鈕程式碼本身完全沒有改動，先前 session 已用真實點擊驗證過可以正常運作）。 |
| 專案架構重構：單檔拆成 ES modules | `index.html`、`css/style.css`、`js/languages.js`、`js/color.js`、`js/ocr-tesseract.js`、`js/ocr-vision.js`、`js/html-builder.js`、`js/preview.js`、`js/main.js` | 使用者確認放棄「雙擊 `index.html` 開啟」的能力後，把原本塞在單一 `index.html`（HTML/CSS/JS 全部寫在一起）的內容拆成多檔案，動機是 PaddleOCR 評估計畫（新增第三種 OCR 引擎）+ 手動編輯介面計畫（可視化調整疊字樣式）都命中了 `CLAUDE.md` 原先訂的「該拆檔」門檻。**採用原生 ES modules，不是 bundler**：`<script type="module" src="js/main.js">` + 檔案間用相對路徑 `import`/`export`，Vercel/dev server 都是走 http（不是 `file://`），不需要 Vite/webpack 就能運作，`index.html` 本身仍是零建置步驟。CSS 也搬到 `css/style.css`。函式簽章順便調整成明確傳參數（例如 `buildFinalHtml(imageDataUrl, altText, detectedLines)`、`renderPreview(previewWrap, detectedLines)`）取代原本依賴模組作用域閉包，方便之後手動編輯介面共用這些函式。**驗證方式**：用 `preview_start` 開 dev server（`.claude/launch.json` 的 `wp-image-seo-tool-dev`），先確認 console 無錯誤、語言選單正確從 `LANGUAGES` 動態渲染出全部 9 個選項；用 canvas 動態產生一張含文字的合成測試圖，走 `DataTransfer` 模擬上傳，確認 OCR 辨識、顏色萃取、位置換算全部正常（`辨識完成，偵測到 1 行文字`）；填 Alt（含 `"`/`<`/`>`/`&` 特殊字元）按「產生 HTML」，讀取 `wpPreviewFrame.contentDocument` 確認渲染文字正確、沒有因跳脫錯誤破版；全程 console 無錯誤訊息。 |
| PaddleOCR 整合為預設辨識引擎（三引擎並列） | `js/ocr-paddle.js`（新）、`js/languages.js`、`js/main.js`、`index.html`、`css/style.css`、`docs/plan-paddleocr-evaluation.md` | 依計畫 Phase A~D 全部跑完（詳細實測數據見計畫文件「七、Phase A 實測結果」）。**Phase A 實測**：`npm install` 兩個候選套件到 scratch 目錄讀 `.d.ts`（比網路調查可靠的第一手 API 來源），寫獨立 spike 頁面在真實瀏覽器跑三方比較——關鍵測試圖是純木紋紋理背景、**完全沒有文字**：Tesseract 誤判出 3 行假文字（重現原始 bug），`@paddleocr/paddleocr-js` 跟 `ppu-paddle-ocr` 兩個套件都正確回傳空結果、零誤判；混紋理+真文字的圖上 `ppu-paddle-ocr` 辨識完全正確（含空格），`paddleocr-js` 漏了一個空格；`ppu-paddle-ocr` 冷啟動只要 3 秒（`paddleocr-js` 要 15 秒，因為預設抓的模型+相依的 `@techstark/opencv-js` 都重很多）。CDN 測試也發現 `esm.sh` 會讓 `paddleocr-js` 噴 `process.binding` 錯誤，換 jsdelivr 的 `+esm` 端點才正常。**Phase B 決定**：選 `ppu-paddle-ocr`（不是 `paddleocr-js`）整合，PaddleOCR 取代 Tesseract 成為預設引擎（textureOnly 測試上 Tesseract 誤判、PaddleOCR 零誤判，是「表現好的那個」的實測依據）。**Phase C 實作**：新增 `js/ocr-paddle.js`，`import()` 從 `https://cdn.jsdelivr.net/npm/ppu-paddle-ocr/web/+esm` 動態載入（只有真的選這個引擎才下載，不拖累另外兩個引擎），輸出跟 Tesseract/Vision 相同的 `{text,x0,y0,x1,y1}` 格式（`ppu-paddle-ocr` 的 `box:{x,y,width,height}` 幾乎零轉換成本）；`MIN_CONFIDENCE_PADDLE=0.3` 當保守安全網（不是像 Tesseract 那樣經驗證實必要的門檻）；`index.html` 的「進階設定」改成三個 radio（PaddleOCR/Tesseract/Google Vision）+ 對應面板依選擇顯示/隱藏；語言選單重新設計——PaddleOCR 預設模型內建涵蓋 CJK+拉丁語系不用勾選，只有真正不同書寫系統（韓文/阿拉伯文/西里爾/希臘/泰文/坦米爾/泰盧固/天城文）才需要從下拉選單切換模型（`PADDLE_SCRIPTS` in `js/languages.js`）；Google Vision 辨識失敗時 fallback 目標從 Tesseract 改成 PaddleOCR。**Phase D 驗證**：`DataTransfer` 模擬上傳測試四個情境——預設 PaddleOCR 辨識合成海報圖（含 Alt 特殊字元跳脫）、切 Tesseract 回歸測試沒壞、Vision 引擎沒填金鑰時正確攔下不送出無效請求、PaddleOCR 書寫系統切到韓文（`V5_KOREAN_MOBILE_MODEL`）正確辨識「안녕하세요」，全程 console 無錯誤。 |
| WordPress 預覽區手動編輯介面 | `js/editor.js`（新）、`js/color.js`、`js/html-builder.js`、`js/preview.js`、`js/main.js`、`index.html`、`css/style.css`、`docs/plan-manual-overlay-editor.md` | 依計畫 Phase A~G 全部跑完（詳細驗證記錄見計畫文件「七、實作結果與驗證記錄」）。**Phase A**：`#wpPreviewWrap` 旁兩條透明度拉桿（底圖/疊字）直接操作 `wpPreviewFrame.contentDocument` 內元素的 `style.opacity`，不碰 `htmlOutput`；疊字拉桿用「乘上各方塊自己原有 opacity」而非直接覆蓋，靠 iframe `load` 事件重新快取每個 `.ovText` 的 `data-baseOpacity` 並把兩條拉桿重置回 100%，天然滿足「重新產生 HTML 後拉桿歸零」的需求。**Phase B**：「手動編輯」按鈕（疊在預覽區右下角）+ 雙擊預覽兩種方式都開啟同一個 `<dialog id="editorDialog">`；雙擊監聽器掛在 `contentDocument`（不是外層 `<iframe>` 元素本身——不同瀏覽環境的 iframe 內容點擊事件不會冒泡到父文件的 iframe 標籤，這點跟計畫原本的假設不同，改成監聽 `contentDocument` 才能真的收到事件）。**Phase C/D**：新增 `js/editor.js`，`editableLines` 是 `detectedLines` 的深複製，用跟 `renderPreview()` 一樣的手法畫可拖曳畫布；文字方塊本身 `contenteditable="true"` 原地編輯，拖曳用 delegated `pointerdown`/`pointermove`/`pointerup`（5px 閾值分辨「點擊」跟「拖曳」，拖曳中暫時關閉 `contenteditable` 避免跟文字選取互搶）；側邊面板即時調字級/顏色（`rgbToHex`/`hexToRgb` 新增到 `js/color.js`）/透明度；「新增文字方塊」插入後自動選取+聚焦+全選文字方便直接輸入取代預設值。**Phase E/F**：編輯介面自己的底圖透明度拉桿（純預覽）；「全部文字改透明」開關在存檔當下批次把每個方塊的 `opacity` 覆蓋成 0（不直接改 `editableLines` 底層值），關閉開關後每個方塊各自恢復自己原本設定的值；「儲存」寫回 `detectedLines` 並同時呼叫 `renderPreview()`（上傳預覽區）與 `generateAndPreview()`（HTML 輸出+ WordPress 預覽）保持兩處畫面同步；「取消」直接關閉對話框，`editableLines` 只是本地複本，不動 `detectedLines`。**架構決策（超出計畫文字但屬於必要延伸）**：新增文字方塊沒有 OCR 邊界框可繼承，`widthPct`/`heightPct` 存 `null`，`buildFinalHtml()`/`renderPreview()` 依此值是否為 `null` 分兩種 sizing 分支（`null` 時 `width:auto;height:auto` 讓內容自然撐開，非 `null` 維持原本 OCR 方塊的固定尺寸 `nowrap+overflow:hidden`）——**只有新增方塊套用新分支，既有 OCR 方塊的尺寸行為完全沒有改動**，避免影響已驗證過的預設輸出。**實作中修正的邊界案例**：用方塊自己的「×」鈕刪掉「非目前選取中」的方塊時，若不調整 `selectedIndex` 會因陣列位移導致面板誤指到別的方塊資料，`deleteLine()` 加了位移調整處理。**驗證方式**：`DataTransfer` 模擬上傳走完整 PaddleOCR 流程，真實瀏覽器測試——拉桿正確操作 iframe DOM 且不寫進 `htmlOutput`；兩種開啟方式都用真實 `PointerEvent`/`MouseEvent` 派發驗證（不是只檢查程式碼存在）；`rgbToHex`/`hexToRgb` 往返一致（`rgb(41, 68, 144)` ↔ `#294490`）；合成 `PointerEvent` 模擬拖曳，位置百分比正確更新，`contentEditable` 拖曳中/後正確關閉/恢復；新增方塊自動選取聚焦全選；全部改透明存檔後 `htmlOutput` 對應行確實是 `opacity: 0`（即使面板當時顯示別的值）；取消後 `htmlOutput`/`detectedLines` 位元對位元不變；全程 console 無錯誤。 |
| PaddleOCR 背景預載 + CDN preconnect（加速首次辨識） | `js/ocr-paddle.js`、`js/main.js`、`index.html` | 使用者反應「能不能一打開網頁就先下載 PaddleOCR」。**做法**：`js/ocr-paddle.js` 新增 `preloadDefaultModel()`，`main.js` 在頁面載入時若目前選的引擎是預設的 PaddleOCR（尊重已存 Google Vision 金鑰而自動選 Vision 引擎的情境，這種情況不會白白預載用不到的 PaddleOCR 模型）就呼叫它，用 `requestIdleCallback(fn, {timeout:2000})` 排程，讓初始渲染優先完成。**除錯過程中發現的重要細節**：一開始沒加 `timeout` 選項，在背景/非聚焦分頁（`document.hidden===true`）下 `requestIdleCallback` 幾乎不會被觸發——瀏覽器刻意大幅延後背景分頁的 idle callback，沒有 `timeout` 保底的話，使用者如果在背景分頁打開這個工具，預載可能永遠不會執行，等於白做；加上 `{timeout:2000}` 後瀏覽器保證最晚 2 秒內強制執行，不管分頁是否可見。**順便修正一個潛在的重複下載 bug**：原本的 `serviceCache` 只快取「已完成」的 service 物件，如果背景預載跟使用者手動上傳幾乎同時發生，兩者都會在 `serviceCache.has()` 檢查時看到還沒有東西、各自重新觸發一次完整下載+初始化，浪費頻寬且最後只有其中一個結果會被保留；改成**快取進行中的 Promise**（`serviceCache.set()` 在第一個 `await` 之前同步執行），兩個呼叫方能正確共用同一次下載，且下載失敗時會從快取移除該 key 讓下次呼叫能重試（不會被一次失敗永久卡死）。另外在 `index.html` `<head>` 加三個 `<link rel="preconnect">`（`cdn.jsdelivr.net`／`media.githubusercontent.com`／`raw.githubusercontent.com`，PaddleOCR 模組跟模型權重分別放在這幾個主機上），讓 DNS/TLS 交握提前跟頁面其他資源並行，不管有沒有觸發預載都有效。**驗證方式**：真實瀏覽器測試，頁面載入後等待幾秒（讓背景預載完成的時間），在單一 script 執行內（避免工具呼叫間的來回延遲污染計時）量測「上傳圖片」到「辨識完成」文字出現的時間，量到 224ms（相較 Phase A 實測的冷啟動 ~3 秒），確認預載確實生效；`read_network_requests` 工具本身觀察不到動態 `import()` 觸發的請求（另外直接測試同一個 CDN URL 的 `import()` 確認能成功載入，排除是程式碼問題，判斷是這個瀏覽工具對 ES module 動態載入的請求記錄有缺口，不是實作缺陷）；全程 console 無錯誤。 |
| 介面大改版：雙欄佈局＋即時編輯＋方塊互動重做（修 3 個 bug） | `index.html`、`css/style.css`、`js/editor.js`（重寫）、`js/main.js`、`js/html-builder.js`、`js/preview.js`（刪除） | 使用者提供佈局草圖＋回報手動編輯介面 3 個 bug，用 Plan Mode 確認方向後執行。**根因診斷**：打不了字／方塊黏游標這兩個 bug 同一個根因——舊版 `js/editor.js` 用「移動超過 5px 判定為拖曳」的閾值分辨點擊跟拖曳，拖曳中還會把 `contentEditable` 關掉再開回來；正常點擊的手部微小晃動很容易超過閾值，導致單純想打字的點擊被誤判成一次極短拖曳吃掉游標；`pointerup` 若沒被乾淨接住（例如指標移出視窗放開），`dragState` 不會被清掉，後續任何滑鼠移動都被當成還在拖曳。**修法是結構性拆開，不是調參數**：文字方塊改成永遠 `contentEditable`、不再有任何程式碼 toggle 它；新增左上角「移動」把手＋選取後四角「縮放」把手，`pointerdown` 只在點中這些把手時才啟動拖曳邏輯，點文字本身完全不會觸發任何拖曳判斷；`pointermove` 額外檢查 `e.buttons===0` 自動清掉卡住的 `dragState`、同時監聽 `pointercancel`。**位置/大小不對準（bug 1）**：不動 OCR 演算法本身，改成把編輯畫布從「按鈕/雙擊才開的彈窗」變成「辨識完成立即顯示在右欄」，讓這個編輯步驟本身就是使用者要求的「最後定位確認」。**取消儲存/取消模型**：`editableLines` 深複製整層拿掉，`js/editor.js` 直接原地修改呼叫方傳入的 `detectedLines` 陣列（`push`/`splice`/屬性賦值），`mountEditor()` 取代 `openEditor()`，不再有 `<dialog>`／`showModal()`／Save-Cancel 按鈕；「全部文字改透明」改成真正即時開關（勾選時暫存每個方塊目前的 `opacity` 到 `_prevOpacity` 再歸零，取消勾選時個別讀回，不是恢復成同一個值）。**新增手動縮放**：取代先前「方塊維持自動不給縮放」的舊決定，`widthPct`/`heightPct` 不再有 `null`/auto-size 分支，統一都是明確矩形，`js/html-builder.js` 跟著簡化掉 `hasBox` 判斷；新增文字方塊給預設矩形（24%×8%），插入後可立即用縮放把手調整。**佈局**：左右兩欄 `grid`（左：純上傳+縮圖，不疊字；右：永遠掛載的編輯畫布），`js/preview.js`（唯讀疊字預覽）因此整個變成死碼直接刪除；辨識引擎/語言設定從 `<details>` 改成預設收合、點擊才彈出覆蓋畫面的浮動選單（點外部或 Esc 關閉）；「產生 HTML」的輸出區塊維持頁面內一般區塊、預設 `hidden`（不做浮動滑出面板，使用者確認過的方向）。**驗證方式**：`DataTransfer` 模擬上傳＋真實瀏覽器測試——右欄在按「產生 HTML」之前就已顯示可編輯畫布；直接點文字用 `computer` 工具真實鍵盤操作可以打字插入（確認修好）；用合成 `PointerEvent` 分別測試「拖曳文字本身」（確認不會移動方塊，修好 bug 3 的根因）跟「拖曳移動把手」（確認正確移動）跟「拖曳四角縮放把手」（NW/SE 兩角都測，確認寬高與錨點座標換算正確）；刪除、新增文字方塊（自動選取聚焦全選）都正常；全部改透明勾選/取消勾選，兩個不同透明度的方塊各自正確恢復（不是恢復成同一個值）；設定選單預設收合、點擊彈出、點外部關閉；產生 HTML 後輸出區塊正確從隱藏變顯示，內容正確反映當下編輯狀態；全程 console 無錯誤。**已知測試限制**：這次自動化測試環境的 `key BackSpace` 動作連在完全沒改過的原生 `<textarea>` 上都無法刪字（已對照驗證排除是這次改動的問題），所以退格刪字這個子症狀沒辦法在這個環境直接證實修好，但已確認插入文字（前述打不了字的主要症狀）修好，且這次的修法是拿掉唯一可能干擾鍵盤編輯的機制（拖曳中切換 `contentEditable`），退格應該一併修好，仍建議使用者實機用真滑鼠鍵盤操作驗證一次。 |
| 視覺主題：跟 Portfolio 對齊的配色＋標題文案＋按鈕彈跳感 | `index.html`、`css/style.css` | 使用者要求標題改「讓圖片文字可選」/副標「令圖片上每個文字都被 WordPress 看見」、拿掉原本最上面的說明文字、介面配色比照 `C:\Portfolio` 淺色模式、區塊標題/按鈕文字用醒目色其餘一般色、字體統一微軟正黑體、比照 Portfolio 加彈跳動態感。**配色來源**：派 Explore agent 讀 `C:\Portfolio\src\app\globals.css` 的 `:root` 區塊（OKLCH 格式，換算成 hex），抓到背景 `#FBF3DC`（暖米白）、卡片 `#EFE5C8`、邊框 `#E3D7B5`、文字近黑、真正的「醒目色」其實是自訂的 `--highlight: #7E6725`（橄欖金，不是 shadcn 慣例的 `--primary`，Portfolio 的 `--primary`反而是純灰階），CTA 按鈕/標題都吃這個 token，所以這次也照樣把 `--highlight` 當唯一醒目色使用，`--primary` 保留當結構用近黑色但沒有大量使用。**標題/按鈕範圍界定**：只有各面板最上面那行真正的「區塊標題」（新增 `.sectionTitle` class 手動標記 4 個：「1. 選擇圖片」「圖片預覽、調整介面」「2. Alt 文字」「3. 完整 HTML」）用醒目色，checkbox/radio 旁邊的說明文字跟面板內部欄位標籤（字級/顏色/透明度等）維持一般文字色——避免把「區塊標題」這個詞泛化成全部 `<label>` 標籤都上色，跟使用者說的「其它就一般文字顏色即可」矛盾。按鈕統一改成「白底+醒目色邊框+醒目色文字，hover 才填滿醒目色+白字反轉」的樣式（沿用既有 `.ovMoveHandle`/`.ovDeleteBtn` 已經在用的「預設描邊、hover 填滿」pattern，不是憑空發明新樣式），取代原本寫死的 `#5542fa` 紫色（一併清掉編輯畫布裡選取外框/移動把手/縮放把手殘留的同一個紫色）。**彈跳動態感**：比照 Explore agent 回報的 Portfolio 全域按鈕互動（`transform:scale(0.92)` 快速回彈 100ms＋放開用 `cubic-bezier(0.34,1.56,0.64,1)` 這種 overshoot 曲線彈回 280ms），套用到所有 `<button>`；設定選單彈出、「產生 HTML」輸出區塊出現時也各加一段用同一條 overshoot 曲線的進場動畫（`@keyframes` 進場，因為兩者都是從 `[hidden]`/`display:none` 出現，`transition` 沒辦法在那個瞬間播放，改用一次性 `animation` 反而更簡單，不用碰 `main.js` 的顯示/隱藏邏輯）。**除錯記錄**：一開始把 `color`/`background-color` 也加進按鈕的 `transition` 清單，結果在「產生 HTML」按鈕從 disabled→enabled 的瞬間，文字顏色卡在 disabled 灰色不會變回醒目色（`border-color` 卻正常切換），追到只有這兩個屬性的轉場會卡住，其他（`transform`/`box-shadow`）都沒事；判斷是這個測試瀏覽器環境對「`:disabled` 偽類切換+CSS 變數驅動的 `color`/`background-color` transition」有渲染異常，不是必要功能，直接把這兩個屬性從按鈕的 `transition` 清單移除（彈跳感只需要 `transform`），問題消失。**驗證方式**：`DataTransfer` 模擬上傳走完整流程，真實瀏覽器讀 `getComputedStyle` 逐項核對——`body` 背景色 `rgb(251,243,220)`＝`#FBF3DC`、字體第一順位是微軟正黑體、`h1`/`.sectionTitle` 顏色都是 `rgb(126,103,37)`＝`#7E6725`、`.hint` 說明段落確認已從 DOM 移除、標題/副標文案逐字核對；按鈕在 disabled→enabled 切換後三個屬性（文字色/邊框色/底色）都正確變成醒目色系（抓出並修掉上述卡色 bug 後才過）；產生 HTML 後輸出區塊、設定選單開關都正常運作，功能沒有因為改 CSS 而跑掉；全程 console 無錯誤。 |
| 產生的 HTML 加上 SEO/GEO 優化（結構化資料、語意標籤、CLS 防護） | `js/html-builder.js`、`js/main.js`、`index.html` | 使用者問「產生的 HTML 是否已經是最適合 SEO/GEO 的形式」，檢查後發現不是，逐項補齊。**檢查出的缺口**：`<img>` 沒有 `width`/`height`（圖片載入前瀏覽器不知道長寬比，會造成版面跳動，是 Core Web Vitals 的 CLS 指標，也是排名訊號之一）；沒有 `loading`/`decoding` 提示；外層只是裸 `<div>`，不是語意化的 `<figure>`；完全沒有結構化資料，Google 圖片搜尋/AI answer engine 只能靠 alt 屬性猜圖片內容；`<img src>` 是 base64 內嵌圖片，Google 圖片搜尋等引擎無法索引 base64 圖片、也無法收進圖片 sitemap（這點是這個工具「純前端、不碰 WordPress API」的架構限制，沒辦法在程式裡真正解決，只能提醒使用者手動補一步）。**修法**：`buildFinalHtml()` 新增 `naturalWidth`/`naturalHeight` 參數（`main.js` 已經有這兩個值，OCR 完成時就記錄了），`<img>` 加上 `width`/`height`（瀏覽器拿來算長寬比防止版面跳動，實際顯示大小仍由 CSS 的 `width:100%;height:auto` 控制，兩者不衝突）＋ `loading="lazy" decoding="async"`；外層 `<div>` 改成 `<figure>`（`margin:0` 蓋掉瀏覽器預設的 `1em 40px`），新增可見的 `<figcaption>` 重複顯示 Alt 文字（不是只藏在屬性裡——可見的圖說對 Google 圖片排名是比 alt 更強的訊號，這是公開的 SEO 常識，不是憑空加的）；新增 `<script type="application/ld+json">` 的 `ImageObject` 結構化資料（`contentUrl`/`caption`/`description`），用 `JSON.stringify` 處理跳脫並額外把 `<` 換成 `<` 防止圖片說明裡如果剛好含有 `</script` 字樣會提早跳出 script 標籤。**沒辦法用程式碼解決、只能提醒的部分**：在「3. 完整 HTML」區塊上方加使用者可見的提示，以及輸出 HTML 本身開頭的註解，都明確講「這是 base64 內嵌圖片，貼上 WordPress 後請額外把圖片上傳到媒體庫，把 `<img>` 網址跟 JSON-LD 的 `contentUrl` 換成媒體庫給的真實網址，才能真正被圖片搜尋索引」——這是唯一無法在工具內自動化完成、必須使用者手動做的一步。**驗證方式**：`DataTransfer` 模擬上傳走完整流程，真實瀏覽器解析輸出的 HTML：`<figure>`/`width`/`height`/`loading`/`decoding` 屬性值逐項核對正確；JSON-LD 用 `JSON.parse()` 解析成功、`@type` 是 `ImageObject`、`caption` 正確；`figcaption` 文字正確顯示；既有的疊字透明度拉桿（`.ovText` 選取器）在新結構下仍正常運作，確認沒有因為外層改 `<figure>` 動到既有功能；全程 console 無錯誤。 |
| 全專案 bug 稽核：靜態預檢腳本 + 人工複查 checklist，修 8 個真實問題 | `scripts/precheck.js`（新）、`docs/manual-review-checklist.md`（新）、`js/main.js`、`js/editor.js`、`css/style.css` | 使用者要求先寫一個覆蓋全專案的預檢腳本抓 bug、再想清楚哪些 bug 是腳本抓不到但不用實跑測試就能推理出來的、寫成 checklist 逐項修。**Part 1：`scripts/precheck.js`**（純 Node 內建模組 `fs`/`path`/`child_process`，不裝任何套件）——JS 語法檢查（`node --check` 逐檔 pipe stdin）、`getElementById`/`getElementsByName` 是否對得上 `index.html` 真的存在的 `id`/`name`、ES module `import` 的具名匯入是否真的在目標檔案 `export`、`<script src>`/`<link href>` 指向的檔案是否存在、`index.html` 有沒有重複 `id`、HTML 標籤配對（stack-based）、CSS `#id`/`.class` 選擇器是不是死的（跟 HTML/JS 對不上）。寫完先故意注入三種假 bug（打錯 id、匯入不存在的 export、語法錯誤）驗證腳本真的抓得到，再還原——這次專案本身跑起來乾淨（唯一一個 `.ovResizeHandle` 的 CSS 死選擇器誤判是我自己字串比對邏輯太天真，用字串串接組出來的 class name 沒被抓到，修檢查邏輯本身，不是專案的 bug）。**Part 2：`docs/manual-review-checklist.md`**——列出「靜態比對抓不到、純讀程式碼推理才找得到」的 bug 類型，逐項在專案裡實際檢查：① **競態條件**（`js/main.js` 模組層級的 `imageDataUrl`/`naturalWidth`/`naturalHeight`/`detectedLines` 被多個上傳流程共用，連續上傳兩張圖、前一張 OCR 還沒跑完就傳第二張，前一張遲到的結果會用「當下已被第二張蓋掉」的尺寸算錯位置、還把第二張的畫面整個覆蓋掉——加 `uploadToken` 遞增計數器，過期的結果全部靜默捨棄）② **缺錯誤處理**（`img.onerror` 完全沒寫，壞檔案會卡死在「載入圖片中…」；`copyBtn` 的 `clipboard.writeText()` 沒包 try/catch，失敗時使用者毫無回饋）③ **邊界夾限不對稱**（縮放有夾下限，但移動完全沒夾範圍，方塊可以拖出 `overflow:hidden` 的畫布外，因為沒有座標數值輸入框、只能用滑鼠抓手把，方塊看不到就永遠抓不回來——移動跟縮放統一夾在 `[0, 100-寬/高]` 範圍內）④ **鍵盤可達性**（移動/縮放把手只認 pointer 事件，純鍵盤使用者完全無法搬動方塊——移動把手取得焦點時加方向鍵微調，Shift+方向鍵幅度加大）⑤ **圖示按鈕缺 `aria-label`**（只有 `title`，補上）⑥ **焦點管理**（刪除方塊時，原本有焦點的按鈕被整個移除，焦點會憑空掉到 `<body>`——刪除後主動把焦點轉移到畫布容器，`index.html` 補 `tabindex="-1"`）⑦⑧ 機密資料外洩／HTML 跳脫一致性兩項純驗證，確認乾淨沒有問題。**驗證第③項時意外在瀏覽器裡抓到一個新 bug**：`handle.setPointerCapture(e.pointerId)` 寫在 `dragState = {...}` 賦值**之前**且沒包 try/catch，這個 API 在瀏覽器判定該 pointerId 不是「作用中指標」時會 throw `InvalidPointerId`（合成 `PointerEvent` 測試重現到了），一旦 throw，`dragState` 永遠不會被設定，整次拖曳從頭到尾都無效、還沒有任何錯誤訊息——`setPointerCapture` 只是「游標移出範圍也持續收事件」的加分優化（`editorCanvasWrap` 上本來就有 delegated 監聽器兜底），失敗不該讓整個拖曳報銷，補 try/catch 靜默忽略。**驗證方式**：`scripts/precheck.js` 改完再跑一次確認 0 錯誤；真實瀏覽器測試——連續上傳兩張不同尺寸的圖（600px 寬 vs 200px 寬），確認最終畫面/文字方塊座標只反映第二張、沒有混到第一張算錯的資料；上傳一個假造的損壞檔案，確認正確跳出「圖片載入失敗」而不是卡住；方塊拖到畫布外幾千 px 遠，確認正確夾限在 0%~(100-寬/高)% 範圍內、拖曳把手加 `setPointerCapture` try/catch 後拖曳確實生效（改之前用合成 pointerId 重現了完全拖不動的情況）；移動把手取得焦點後方向鍵/Shift+方向鍵正確微調位置；刪除方塊後焦點正確落在畫布容器而不是 `<body>`；`aria-label` 屬性值核對正確；複製按鈕在沒有 clipboard 權限的測試環境下正確顯示失敗訊息而非靜默無反應；全程 console 無錯誤。 |
| 介面微調＋編輯彈窗回歸（右欄改唯讀預覽＋雙擊/按鈕開編輯視窗）＋文案/圖示修正 | `index.html`、`css/style.css`、`js/main.js`、`js/preview.js`（新，復用先前刪除的唯讀預覽模組）、`js/editor.js`（不改，只改掛載時機） | 使用者根據截圖回報 4 個佈局問題＋2 個架構調整＋3 個文案/圖示要求。**佈局**：標題列右側新增 `.headerRight` 把「Created by 彈羊如柳丁」跟設定按鈕收在一起、置中對齊在右側空白處（原本設定按鈕單獨飄在最右上角）；`#previewWrap`（左欄縮圖）從「有圖才 `display:block`」改成一開始就 `flex:1` 撐滿，`.mainGrid` 加 `align-items:stretch` 讓左右兩欄等高（用 `getBoundingClientRect()` 驗證兩欄高度完全相等）；右欄標籤拿掉括號說明只留「圖片預覽」，圖片放入前只顯示空白區域（`openEditorBtn`/`previewOpacityControls` 預設 `hidden`，選圖後才顯示）；`.bottomRow` 改回 `grid-template-columns:1fr 1fr` 讓 Alt 文字區塊維持左半寬、不再撐滿整行（驗證 `altPanelWidth` 剛好是 `bottomRowWidth` 一半）。**架構調整（使用者明確要求把先前改版拿掉的彈窗式編輯器局部復原）**：先前大改版把編輯畫布改成「辨識完成立即顯示、直接可編輯」，這次使用者澄清「剛插入圖片時右邊跳出的地方不是要直接編輯，只是要調整預覽、透明度」——重新拆成兩層：復原 `js/preview.js`（唯讀疊字預覽，`.previewLine` 純 `position:absolute` 文字、無把手無 `contenteditable`，辨識完成後＋每次編輯視窗關閉後呼叫 `renderPreview()` 更新）掛載在右欄 `#previewCanvasWrap`，實際編輯改成點 `#openEditorBtn` 或雙擊預覽區才 `editorDialog.showModal()` 彈出（`js/editor.js` 內部邏輯完全不動，只改成從 `main.js` 的 `openEditor()` 呼叫 `mountEditor()` 才掛載，不再是辨識完成自動掛載）；**但保留即時生效的資料模型**——彈窗內編輯依然直接原地修改 `detectedLines`，沒有恢復 Save/Cancel 按鈕，只有一個「完成」鈕負責關閉＋同步唯讀預覽，是先前「彈窗觸發」與「即時編輯」兩個方向的折衷。縮放把手改實心、縮小（`.ovResizeHandle` 從 `12px` 空心邊框改 `8px` 純色 `background:var(--highlight)` 無邊框）。**文案/圖示**：Alt 標籤拿掉括號說明；WordPress 預覽區兩條拉桿標籤「透明度」訂正為「不透明度」（滑桿控制的是不透明度數值，跟拖曳阻力/穿透的「透明度」用詞不同，訂正全部相關 label）；`assets/icon/orange-sheep.png` 設成 `<link rel="icon">` favicon；同一張圖加在「Created by 彈羊如柳丁」文字右側（`.creditLogo`，20×20px）。**除錯過程**：改用 `editorDialog.addEventListener('close', syncPreviewFromEditor)` 當唯一同步時機，測試時發現點「完成」關閉對話框後唯讀預覽沒有更新；逐步排除——先確認資料本身確實有改到（重開對話框看得到編輯結果，不是 `editor.js` 的資料 bug）、再直接測試 `close` 事件本身有沒有觸發（加 listener＋等待，`fired:0`）、最後用一個完全獨立、跟這個專案無關的最小 `<dialog>` 從頭建立測試（`createElement('dialog')`→`showModal()`→`close()`），一樣量到 `fired:0`，但 `dialog.open` 正確變成 `false`——判定是這個測試瀏覽器環境本身 `<dialog>` 的 `'close'` 事件沒有確實觸發，不是專案程式碼的 bug；修法是 `closeEditorBtn` 的 click handler 除了 `editorDialog.close()` 之外**同時同步呼叫** `syncPreviewFromEditor()`，不再只靠 `close` 事件驅動這條使用者最常用的路徑（`close` 事件監聽器仍保留當備援，處理 Esc 鍵關閉這種沒有經過按鈕點擊的情境）。**驗證方式**：`node scripts/precheck.js` 改動前後皆 0 錯誤；真實瀏覽器測試——選圖前右欄除了「圖片預覽」標籤外完全空白（無按鈕無拉桿）；選圖辨識完成後唯讀預覽立即顯示疊字文字、但點擊/雙擊文字本身不會進入編輯（確認唯讀）；點「✎ 編輯」按鈕跟雙擊預覽區都能正確開啟編輯視窗；編輯視窗內修改文字/位置後按「完成」，唯讀預覽同步更新（修好上述 `close` 事件不觸發的問題後才過）；產生 HTML 正確反映編輯視窗裡的最終編輯結果；`previewCanvasBgOpacity` 拉桿即時改變唯讀預覽底圖透明度；縮放把手 `getComputedStyle` 核對 `width/height:8px`、`background-color:rgb(126,103,37)`、`border-style:none`；`getBoundingClientRect()` 核對 `altPanelWidth`＝`bottomRowWidth` 一半、`uploadPanelHeight`＝`editPanelHeight`；favicon 與 credit logo 皆 `naturalWidth/naturalHeight:500`、`complete:true` 成功載入；全程 console 無錯誤。 |
| 修文字對齊根因 bug＋字距/行距拉桿＋自動合併相近 OCR 行＋主副標合一行 | `js/html-builder.js`、`index.html`、`css/style.css`、`js/editor.js`、`js/preview.js`、`js/main.js`、`js/line-merge.js`（新） | 使用者回報「產生的文字仍然無法準確對齊圖片中的文字位置」，要求繼續查根因；另外要求標題主副標合成一行、新增字距/行距調整功能、OCR 偵測到行距很短的文字行應視為同一個文字方塊。**對齊根因診斷**：寫一個合成測試——用 `canvas.fillText()` 在已知座標畫一行文字、用 `ctx.measureText()` 的 `actualBoundingBox*` 算出真實 bounding box 當 ground truth，餵進真正的 OCR pipeline 跑一次，比對「OCR 偵測到的 %」跟「真實 %」的差距，藉此把「OCR 本身不準」跟「程式碼座標轉換有 bug」兩種可能分開來看。結果：OCR 偵測誤差本身很小（<1%），但透過 `getBoundingClientRect()` 比對容器高度後，發現**兩個真正的座標系統 bug**：① 右欄唯讀預覽 `#previewCanvasWrap` 之前是 `flex:1` 直接撐滿面板高度（為了「跟左欄等高、不留空白」），但 `.previewLine` 的 `top`/`height` 百分比是相對這個被撐高的容器算的，容器實際比圖片本身的顯示高度多出一截（實測 321px vs 圖片實際 238px），造成疊字整體往下偏移；② `buildFinalHtml()` 產生的 `<figure>` 本身身兼「定位容器」+「包住 `<figcaption>`」雙重角色，`<figcaption>` 是版面內文字會撐高 `<figure>`，但 `.ovText` 的 `top`/`height` 百分比是相對 `<figure>` 算的，於是也被 `<figcaption>` 的高度污染（實測 526px vs 圖片實際 501px）——**這個 bug 連使用者最終貼到 WordPress 的 HTML 都受影響，不只是預覽畫面**。**修法**：兩處都改成「定位容器只包住圖片本身，不包住任何會撐高的額外內容」——`index.html` 把 `#previewCanvasWrap` 拆成外層 `#previewCanvasOuter`（負責 flex 撐滿、置中）+ 內層 `#previewCanvasWrap`（純粹包圖片，高度永遠等於圖片實際顯示高度，不再被 flex 拉高）；`html-builder.js` 把 `<figcaption>` 移到定位用的內層 `<div>` 外面，`<figure>` 只當外層包裝，不再是定位容器本身。**驗證方式**：修完重跑同一組合成測試，`previewCanvasWrap` 高度／最終 HTML 裡定位容器的高度都跟圖片實際顯示高度**完全相等**（改之前分別差 34px／25px），確認兩處都修好。**過程踩到的環境陷阱**：第一次改完在瀏覽器測還是量到跟改之前一樣的錯誤數字，一度以為改壞了——後來直接 `fetch(no-store)` 讀伺服器實際檔案內容確認新程式碼已經正確寫入磁碟，才意識到是這個測試瀏覽器工具本身把 ES module 的 `import` 快取住了，連 `Ctrl+Shift+R` 都沒完全洗掉，最後換一個全新分頁＋換一個新的本機伺服器 port 才量到正確結果——純屬這次驗證工具的環境限制，跟程式碼無關。**主副標合一行**：`<h1>` 改成「讓圖片文字可選：<span class="subtitle">令圖片上每個文字都被 WordPress 看見</span>」，原本獨立的 `<p class="subtitle">` 拿掉，`.subtitle` 樣式從區塊級調整成行內（字級縮小、一般字重、半透明），跟主標題同一行但視覺上仍可分辨主副。**字距/行距拉桿**：`detectedLines` 新增 `letterSpacing`（em，預設 0）／`lineHeight`（倍數，預設 1.05）兩個欄位，`#editorPanel` 新增對應兩條拉桿（字距 -0.05~0.5em、行距 0.8~2.5），套用到 `js/editor.js`（即時編輯畫布）／`js/preview.js`（唯讀預覽）／`html-builder.js`（最終輸出）三處渲染，同時把三處原本寫死的 `line-height:1.05` 跟 `white-space:nowrap` 改成用資料驅動＋`white-space:pre-line`（為了下面的自動合併多行文字方塊鋪路——`nowrap` 沒辦法顯示合併後文字裡的換行）。**自動合併相近 OCR 行**：新增 `js/line-merge.js` 的 `mergeCloseLines()`，OCR 完成後、渲染之前呼叫——把偵測到的文字行依 `topPct` 排序，相鄰兩行若垂直間距小於兩行平均高度的 50%、且水平範圍重疊至少 40%，判定是同一段落，合併成一個方塊（文字用 `\n` 接起來、bounding box 取聯集），不相關的文字行（例如頁尾跟頁首）維持分開。合併後的方塊也會算一個貼近原始行距的預設 `lineHeight`（不是沿用單行的 1.05）——因為 `fontSizeCqw` 是相對圖片「寬度」的百分比、`heightPct` 是相對圖片「高度」的百分比，兩個單位不同軸，換算行距倍數時要用圖片的寬高比橋接兩者，`line-merge.js` 內有完整換算公式跟來源說明。**驗證方式**：合成測試圖畫兩行間距很近的文字＋一行離很遠的文字，上傳後確認前兩行正確合併成一個方塊（`text` 含 `\n`、`lineHeight` 算出約 1.33 而非預設 1.05）、第三行維持獨立方塊；在編輯視窗手動調整字距/行距拉桿，確認畫布上的方塊即時套用、關閉視窗後唯讀預覽同步、按「產生 HTML」後最終輸出的 `.ovText` 也正確帶有 `white-space:pre-line`／`line-height`／`letter-spacing`；`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。 |
| 捲動動畫：區塊進出視窗淡入淡出＋頂/底端橡皮筋回彈 | `index.html`、`css/style.css`、`js/scroll-effects.js`（新） | 使用者要求比照 `C:\Portfolio` 加「往下滑區塊淡出」與「觸碰到底部產生回彈」兩個捲動互動。**先查 Portfolio 實際怎麼做**（派 Explore agent 讀原始碼，不是憑印象猜）：① 淡出效果實際上不是連續跟著捲動百分比算透明度，而是 Framer Motion `whileInView({opacity:1}, {once:false})`——區塊離開視窗（不論往上或往下捲出）就淡出、捲回來再淡入，底層是 `IntersectionObserver`；② 回彈效果是獨立元件 `OverscrollBounce`（`C:\Portfolio\src\components\overscroll-bounce.tsx`），因為 Portfolio 用 Lenis 平滑捲動函式庫接管了捲動、瀏覽器原生的橡皮筋效果會被蓋掉，所以自己用 wheel/touch 事件手動重建：在頂/底端偵測到繼續往外捲的滾輪/觸控輸入時，把整個頁面用 Framer Motion 的 `useMotionValue` 拉出最多 80px（`RESISTANCE=0.4` 阻力、`DEAD_ZONE=10px` 死區），停止輸入 75ms 後用 `bounce:0.35` 的彈簧動畫彈回 0。**這個專案沒有 React/Framer Motion/Lenis，兩個效果都用原生 JS/CSS 重寫**，行為對齊但實作換一套：淡出用瀏覽器原生 `IntersectionObserver`（`rootMargin:'-40px'` 對應 Portfolio 的 `viewport:{margin:'-40px'}`），套用在四個主要區塊（標題列／上傳面板／預覽面板／Alt+產生按鈕列，`#outputWrap` 刻意不套——它自己已經有「產生 HTML」時的一次性進場動畫 `fadeSlideUp`，疊加第二個 opacity/transition 會互相打架）；回彈用 `#pageBounceWrap`（包住除了 `<dialog>` 外的整個頁面內容，dialog 走瀏覽器原生 top-layer 渲染，天生不受祖先 transform 影響，不需要特別排除）搭配純 `translateY` inline style，拖曳中即時跟手（`transition:none`）、放開後才切成 `transition:transform 500ms cubic-bezier(0.34,1.56,0.64,1)`——這條 overshoot 曲線是直接沿用這個專案既有的按鈕彈跳曲線（見上方「視覺主題」那筆），不是另外調的，維持全站彈跳手感一致；`html,body` 加 `overscroll-behavior:none` 蓋掉瀏覽器原生回彈，避免原生+自製雙重回彈。另外補一個 Portfolio 沒有的防呆：`<dialog>` 開啟時（`js/editor.js` 的編輯視窗）wheel/touch 事件直接略過，避免背景頁面在彈窗開著時還在回彈。**驗證方式與這次踩到的環境限制**：`node scripts/precheck.js` 0 錯誤；縮小視窗到 1000×500 讓頁面可以捲動，捲動邏輯本身（`atTop`/`atBottom` 邊界判斷、`wheel`/`touchmove` 事件是否正確攔截、`preventDefault()`、pull/dead-zone 數學、`<dialog>` 開啟時是否正確略過）都用合成 `WheelEvent`/直接呼叫驗證正確；但**這次測試瀏覽器工具的分頁完全不跑 render steps**（`requestAnimationFrame` 排的 callback 等了 1 秒也沒被呼叫，這不是這次新發現的孤例，跟本專案先前已經記錄過的「`<dialog>` close 事件不觸發」「`requestIdleCallback` 背景分頁被延後」屬於同一類分頁背景/非前景時的限制）——這代表**CSS transition 的動畫過程、`IntersectionObserver` 的 callback 本身都沒辦法在這個工具裡直接觀察到**（兩者都是瀏覽器 render steps 的一部分）。改用 `transition:none` 強制瞬間套用來驗證 CSS 規則本身正確（`.scrollFaded` 確實產生 `opacity:0; transform:translateY(16px)`，套用前後手動比對過），JS 判斷邏輯（class 有沒有正確被加上/拿掉、`translateY` 數值算得對不對、idle 後 `setTimeout` 是否正確觸發 `release()`）則不依賴 render steps、可以正常驗證且全部通過；**動畫本身流不流暢、`IntersectionObserver` 在真實捲動時會不會確實觸發**，都需要使用者實機用真瀏覽器確認（見下方待辦）。 |
| 修好合併行邏輯真的 bug＋編輯視窗方塊互動改成 PPT 式 | `js/line-merge.js`、`js/editor.js`、`css/style.css` | 使用者用真實行銷圖（中秋禮盒海報）回報自動合併相近文字行的功能把幾乎整張圖的文字（大標題＋副標＋三組小圖示說明文字）全部誤合併成一塊，貼上編輯視窗後文字擠成一團亂碼疊在一起。**先分析根因（使用者要求暫不動手）**：`js/line-merge.js` 原本的合併判斷完全沒檢查兩行「字級是否相近」，間距門檻用兩行「平均高度」去算——標題（大字）跟緊接著的副標（小字）平均起來門檻被拉得很寬鬆，兩者被誤判成同一段；而且合併後 `prev.heightPct` 會被改寫成累積範圍的高度，下一輪比較門檻又更寬，造成連鎖誤合併一路吃到底部所有圖示說明文字。使用者確認以圖中版面手動列出「應該有六塊」（標題2行合1塊、副標1塊、右上緞帶「送禮首選」1塊、三組圖示說明各2行合1塊）並要求依此修正方向動手。**修法**：`MIN_HEIGHT_RATIO=0.7` 字級比值門檻（兩行高度較小值/較大值需 ≥0.7 才視為同字級，對應「0.7~1.4 倍」）；間距門檻改用兩行高度**較小值**而非平均值；另外用新增的 `_lastHeightPct` 欄位追蹤「最後一次併入的單行高度」（而非累積後的總高度）做字級比對，避免 3 行以上的正常合併被自己的累積高度誤傷。**同時處理使用者回報的另一個問題**：編輯視窗方塊互動整個換成貼近 PowerPoint 的模式，取代舊的圓形移動把手（✛）＋角落限定 4 個縮放把手＋右上角 × 刪除鈕——新增 8 個小實心控點（四角＋四邊中點）可縮放；新增 `.ovBoxFrame`（緊貼文字外緣 5px 的一圈框線，不內縮文字本身，靠 `z-index` 分層讓文字永遠贏得點擊，框線範圍永遠是文字未覆蓋到的地方，兩者互不干擾，不需要「拖曳多少像素才算拖曳」這種容易誤判點擊/拖曳的判斷法）——拖曳這圈框線移動整個方塊；邊框樣式用 CSS `:focus-within` 驅動：文字聚焦（輸入中）時虛線，方塊被選取（點框線，文字自動失焦）時實線；選取狀態下按 Delete/Backspace 直接刪除方塊，輸入文字時同樣按鍵維持正常刪字（靠鍵盤事件的 `e.target` 落在 `.ovBoxFrame` 還是 `.ovBoxText` 分流，兩者是完全不同的 DOM 元素）；Esc 可以從輸入狀態退回選取狀態。**驗證時額外抓到一個真的程式碼 bug**：判斷「文字目前是否正在被輸入」原本用 `box.querySelector('.ovBoxText:focus')` 這個 CSS selector 查詢，但這個查詢在測試環境下不可靠（`document.activeElement` 已正確指向該元素、`:focus-within` 在祖先元素上也正確判定為 true，唯獨 `:focus` 直接查詢抓不到），改成直接比對 `document.activeElement === textEl`，更直接也更不依賴瀏覽器對這個特定 pseudo-class 的即時性。**驗證方式**：`mergeCloseLines()` 用合成資料驗證——標題+副標+雙圖示說明文字的合成情境正確分成三種字級各自合併（不再全部黏在一起）；4 行同字級合成資料正確全部合併成一塊、且不受累積高度影響（證實 `_lastHeightPct` 修法有效）。編輯器互動用合成 `PointerEvent`/`KeyboardEvent` 逐一驗證：文字聚焦→框線虛線、點框線→文字自動失焦＋框線變實線、拖曳框線→方塊正確位移、拖曳邊中點控點（`e`）→只改寬度不改高度/位置、拖曳角落控點（`nw`）→寬高位置四個值同時正確變化、文字聚焦時按 Backspace→方塊不受影響、框線聚焦時按 Delete→方塊正確刪除。`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。 |
| 修透明文字選不到＋預檢腳本擴大到全專案＋人工複查全跑一輪＋架構評估 | `css/style.css`、`js/editor.js`、`scripts/precheck.js`、`docs/manual-review-checklist.md`、`CLAUDE.md` | 使用者一次提出四件事：① 回報「文字完全透明時選取不到」的嚴重問題；② 檢查 `scripts/precheck.js` 有沒有覆蓋全專案，沒有就調整；③ 評估整份專案架構有沒有優化空間；④ 找出專案裡的除錯流程文件，確認是否適用現有架構、照著跑一輪找 bug 全部修掉。**① 透明文字選不到**：追查發現是前一輪 PPT 式方塊互動改版的副作用——`.ovBoxFrame` 平常邊框全透明、只有 hover 才顯示，當方塊文字本身也是 `opacity:0`（「全部文字改透明」是刻意支援的正常情境）時，畫面上完全沒有東西可以點到。**先確認這不影響核心功能**：寫測試證實 `opacity:0` 不影響瀏覽器原生 `Range`/`Selection` API 選取文字，最終產生的 HTML 本身的可選取性/可複製性沒有問題（這正是這個工具「讓圖片文字可選」的核心承諾），bug 純粹在編輯介面的可發現性上。**修法**：`.ovBoxFrame` 邊框預設從全透明改成 `border:1px dashed rgba(0,0,0,0.18)` 淡淡的常駐提示線，不受文字透明度影響，不管方塊選取/文字透明與否都能被找到並點擊。**② precheck.js 擴大覆蓋範圍**：原本只掃 `js/`（非遞迴）＋`index.html`＋`css/style.css`；新增 PowerShell 語法檢查（`docs/archive-done.ps1`，用 `[System.Management.Automation.Language.Parser]::ParseFile` 純解析、不會真的執行這支會動檔案的腳本）與 Markdown 相對連結檢查（`CLAUDE.md`／`README.md`／`實作進度.md`／`docs/*.md` 全部，連結目標檔案不存在就報錯，略過外部連結跟純錨點連結）；JS 掃描也從 `fs.readdirSync` 單層改成遞迴 `findFiles()`，未來新增子目錄自動涵蓋不用改腳本。跑過擴充後的版本：12 個 JS 檔＋`index.html`＋`css/style.css`＋1 個 `.ps1`＋8 個 `.md`，全部 0 錯誤 0 警告。**③ 架構評估**（純分析報告，詳見對話回覆，只實作了其中零成本的文件修正部分）：`main.js`（485 行）／`editor.js`（453 行）明顯比其他檔案肥大很多（其他都在 200 行以下），`main.js` 尤其違反了 `CLAUDE.md` 自己訂的「不要把新邏輯塞回 main.js 讓它重新肥大」原則；`resetWorkspace()` 跟上傳流程 `reader.onload` 開頭有 6 行重複的 UI 重置邏輯，可以抽共用函式；沒有自動化測試框架，完全靠 `precheck.js`＋人工複查清單＋每次手動瀏覽器驗證，工具規模尚可接受但沒有回歸測試保護網；`CLAUDE.md` 的架構圖已經過時（12 個 js 檔只列了 7 個，缺 `editor.js`／`ocr-paddle.js`／`line-merge.js`／`text-fit.js`／`scroll-effects.js`），已直接補上，順便在「測試方式」補上 `precheck.js`／`manual-review-checklist.md` 的說明（原本完全沒提到這兩個後來才加的工具）。**④ 除錯流程文件複查＋執行**：找到 `docs/manual-review-checklist.md`（先前的人工複查清單，10 個項目：race condition／錯誤處理／夾限／鍵盤可及性／aria-label／focus 管理／金鑰不外流／HTML 跳脫／API throw 防呆／focus trap）。逐項對照現有架構重新核對——文件裡有多處直接引用已經被移除的 `.ovMoveHandle`／`.ovDeleteBtn`（前一輪 PPT 式改版拿掉的），先更新成現有的 `.ovBoxFrame`／8 控點／鍵盤 Delete 架構。照著清單重新推理一遍，9 項確認架構改版後依然乾淨，但抓到兩個真的新 bug：**(a)** `.ovBoxFrame:focus { outline: none; }` 把瀏覽器預設焦點框拿掉卻沒有補任何替代提示，鍵盤使用者 Tab 到方塊時完全看不出焦點在哪個方塊上（可能不小心按 Delete 刪錯方塊），已加 `.ovBoxFrame:focus-visible { outline: 2px solid var(--highlight); outline-offset: 2px; }`（用 `:focus-visible` 只在鍵盤導覽時顯示，滑鼠點擊不會觸發）；**(b)** Tab 到方塊框線（沒真的點擊過）不會更新側邊字級/顏色調整面板，跟滑鼠點擊的行為不一致，已補上 `frame.addEventListener('focus', () => selectLine(index))`。清單新增第 11、12 項記錄這兩個新發現，並修掉數處引用到已移除元素的過時描述文字。**驗證方式**：合成 `PointerEvent` 確認方塊未選取＋文字 `opacity:0` 時框線邊框依然不透明（可發現）；讀取實際 CSS 規則文字確認 `:focus-visible` 規則正確定義並存在於樣式表；用跟既有已上線的 `textEl` focus→selectLine 相同的測試手法驗證新的 frame focus→selectLine 邏輯本身寫法正確（測試環境本身不會對程式化 `.focus()` 觸發 `focus` 事件，這點透過對照既有功能得到一樣的結果來確認是環境限制、不是新程式碼的問題）；完整跑一次「上傳→OCR→開編輯→拖曳/縮放/刪除→產生 HTML」全流程確認沒有回歸；`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。 |
| 小圖放大再辨識＋工具本身網站技術 SEO 補強 | `js/image-prep.js`（新）、`js/main.js`、`index.html`、`robots.txt`（新）、`sitemap.xml`（新） | 使用者確認要落地先前討論過的「放大小圖再辨識」，並要求依照三張 SEO 投影片（資訊架構/HTML/結構化資料、Core Web Vitals/RWD、搜尋友善 CMS 七要素）補強這個工具「自己」這個網站的技術 SEO（不是產生的疊字 HTML 那份，是這個工具本身的首頁）。**小圖放大再辨識**：新增 `js/image-prep.js` 的 `prepareImageForOcr()`，圖片長邊小於 1600px（`TARGET_LONG_EDGE_PX`）時用 canvas 放大到目標長邊（最多 `MAX_UPSCALE_FACTOR=3` 倍，避免小縮圖被硬拉到失真），已經夠大的圖片完全不處理；`main.js` 在呼叫三個 OCR 引擎之前先跑這一步，辨識完拿到的座標是「放大後圖片」的像素空間，換算成 `leftPct`/`topPct` 之前先依同一個比例除回去，換回「原圖」像素空間——這樣下游所有邏輯（百分比計算、`extractTextColor()` 顏色取樣用的是原圖尺寸的 canvas）完全不用改，只在 OCR 呼叫前後插入兩行轉換。**技術 SEO**：對照三張投影片，篩出真的適用單頁工具的項目（跳過投影片裡電商實體/XML sitemap 多頁階層/廣告規範等不適用的部分）：`<meta name="viewport">`（原本整個網站完全沒有這個標籤，會影響手機版面正確性，不只是 SEO 問題）、`<meta name="description">`、`<link rel="canonical">`、Open Graph／Twitter Card 標籤、`WebApplication` 型別的 JSON-LD 結構化資料（用 WebApplication 而非投影片提到的一般 Article/電商 Schema，因為這個頁面本身就是一個免費工具，不是描述工具的文章）；主要內容包一層語意化 `<main>` 標籤幫助內容架構的區隔；新增 `robots.txt`（`Allow: /` 並指向 sitemap）與只有一筆 URL 的 `sitemap.xml`。**驗證方式**：合成資料驗證 `computeUpscaleFactor()` 的四種情境（小圖放大到上限/大圖不處理/剛好卡在門檻/極小圖被 3 倍上限夾住）；用合成的 200×100 小圖跑完整「上傳→放大→辨識→換算回原圖座標」流程，確認最終產生的百分比座標落在合理範圍（沒有跑出 0~100% 之外，證實座標轉換方向正確）；真實瀏覽器核對 viewport/description/canonical/og:title 的實際內容、`JSON.parse()` 驗證 JSON-LD 語法合法且 `@type` 正確、`<main>` 正確包住著陸頁跟工作區兩個區塊；縮到手機視窗尺寸（375px）確認沒有水平溢出；直接 `fetch()` 讀 `/robots.txt`／`/sitemap.xml` 確認內容正確可存取；`node scripts/precheck.js` 全程 0 錯誤；全程 console 無錯誤。 |
